# codexapp 本地开发环境交接

> 本仓库是 `codexapp`（Codex 的轻量级浏览器 Web UI，跑在 Codex app-server 之上）。本文档记录本地环境当前状态、启动方式、历次会话解决的「Codex CLI not found」与页面转圈问题、各轮验收进展，以及交接时需要注意的事项。

**当前快照**

| 项 | 值 |
|---|---|
| Git 分支 | main（round-86（未推送）：上翻更早轮次改为游标分页（去掉最后一处全量水合）——round-84 修掉「打开会话全量水合」、round-85 修掉管道里白跑的会话日志恢复后，`readThreadForTurnPage`（上翻更早轮次用的 `/codex-api/thread-turn-page`）仍发 `thread/read {includeTurns:true}` 再在内存 `slice`；直连 app-server 量到该请求 **1295ms / 12.12MB**，而 `includeTurns:false` 只要 **2ms / 0.00MB**（成本 100% 在构造轮次，与 round-84 同病）。**并纠正 round-84/85 的错误措辞**：两轮遗留都写「顶层 `turnsBackwardsCursor` 正是该用的游标」，实测**是错的**——它 `includeAnchor:true`、用它 `desc` 只会**原样重发自己那一页**（原地打转），真正往更老走的是 `initialTurnsPage.nextCursor`（`includeAnchor:false`）；游标是**不透明 JSON**（`{"requestedThreadId":…,"rolloutOrdinal":N,"includeAnchor":false,"scope":{"kind":"turns"}}`），**把 turn id 当 `cursor` 会被拒**（`invalid cursor: <id>`），即**没有「给我某一轮之前那一页」的一次性调用，只能逐页串链**。设计三步（新增 `src/server/bridge/threadTurnPage.ts`）：①`readThreadTurnIds` 发 `turns/list {10000, notLoaded, desc}`（2–14ms / 0.00MB）反转成升序 id 列，一次拿到锚点下标 / `startTurnIndex` / `hasMoreOlder` / 锚点是否存在（**故意不缓存**，每轮拿最新真相）；②`ThreadTurnPageCursorChain` 按 `threadId → (turnId → 更老游标)` 记录页边界（64 线程 × 每线程 256 条 LRU），来源只有「打开会话那页」与「本路由服务过的每页」；③命中边界就发 `turns/list {cursor, desc, limit, itemsView:'full'}`、反转成升序，**必须校验**页长与页首页尾（游标会活得比指向的轮次更久，校验让陈旧游标只换一次回落而不是一页错误对话）。**任何意外一律返回 null → 回落原全量水合，回落路径与改动前逐字相同。** 元数据等价性先验过：`thread/read {includeTurns:false}` 的 `thread` 对象 **32 键全同、除 `turns` 外值逐字相同**，只换 `thread.turns` 形状零改动。验证：新增单测 24 例、`scripts/probe-turn-page.cjs` **11/11 PASS**、`git stash` 端到端 A/B 四锚点**响应逐字相同**（含字节数）、**真实 Linux（WSL2）全量 649/649 通过**（关掉从 round-73 挂到现在的「2 例 Windows 环境性失败」注记）、`SHARED_BRIDGE_VERSION` v3→v4）。round-85（未推送）：会话日志时序恢复的适用范围——round-84 之后 app-server 侧只剩 198ms、经桥仍 734–1242ms，逐段打断点量出最大的一段是命令合并（**434–681ms** = `readFile` 167–295 + 解析并合并 220–348），而它对当前形状日志**识别到 0 行**：CLI 把命令从 `function_call`/`function_call_output`（`exec_command`/`shell_command`）换成 `custom_tool_call`/`custom_tool_call_output`（`exec`），本机最新最大的两个会话（30.89MB / 2.93MB）识别 0 条、新形状 222/7 条，更早的十个（0.08–0.88MB）才可用；更糟的是没有命令槽时恢复**仍**把 user/agent 提到前面、命令全部追加轮末，**抹掉 app-server 已经正确的流式交错**（`tool-call-blocks-end-of-conversation` 的预期第 2 条在新形状下被违反）。改为两处：①**形状闸门**——`buildSessionItemOrder` 回报 `sawRecoverableToolRow`（判定在轮次过滤**之前**，描述整份日志），`mergeSessionCommandsIntoTurns` 逐轮判断槽里既无 `commandExecution` 也无 `fileChange` 就原样返回该轮，全部轮无改动时返回**入参同一引用**；②**版本判据缓存**——`sessionLogRecoveryCache`（path → `{size,mtimeMs,recoverable}`，上限 64）+ 新入口 `mergeSessionCommandsIntoTurnsFromPath`，判据为假只付一次 `stat` **不读文件**，size/mtime 变则重判，`threadRoutes` 的 live-state 读取共用同一入口。**排除过的方案**：用原文里的 `exec_command` 字样做廉价判据不成立（该 30.89MB 日志里 `exec_command` 出现 317 次、`apply_patch` 56 次，全是命令输出文本里被 grep 到/被打印的内容，不是行类型），判据只能读已解析的 payload。验证：同环境 `git stash` A/B 热态中位数 **1071→610ms（−43%）**，修复后最大值 668ms < 修复前最小值 889ms（两组无重叠）；条目顺序与「跳过合并」基线 **10/10 逐字一致**；旧形状两会话经桥仍注入 **58/58、51/51** 条 `session-cmd-`；新增 `scripts/probe-session-log-recovery.cjs` 固化判据（并含「≥1MB 日志的 stat 必须比读+解析便宜 20 倍」断言）。round-84（未推送）：打开会话去掉全量历史水合——用户口径「慢的不是 DOM，等待时间全在 RPC」；`thread/resume` 让 app-server 全量水合历史后被桥裁到 10 轮，实测 **2118ms / 12.12MB** 全部白付（`includeTurns:false` 只要 2–3ms，成本 100% 在构造轮次）；改为请求 `{excludeTurns, initialTurnsPage{limit:10, sortDirection:"desc", itemsView:"full"}}`（协议原文已把全量水合标 deprecated 并指向这一对调用），桥把返回页**反转成升序**提升为 `thread.turns`、补 `threadTurnStartIndex`、丢弃 `initialTurnsPage`，响应形状与前端归一化**零改动**；旧 app-server 忽略 `initialTurnsPage` 时按原请求重放兜底（避免空对话）；同桥上 A/B **2.0–3.1×**、输出逐字一致；新增 `scripts/probe-resume-turn-page.cjs` 固化协议假设。round-82：systemd 停止超时修复——升级后的 websocket socket 脱离 http server 的 tracked connections，`server.close()` 永不回调，已改为显式 `terminate()` 客户端 + 补齐连接清理；已随 **v0.1.124** 发布。round-79 / round-80 / round-81 已随 **v0.1.123** 发布（同批）。round-81：已消失线程的救济路径——round-80 的「事后修不回来」结论被实测证伪（直写 sqlite 的 `preview` 列无效，rollout 才是元数据事实源），改用 `thread/resume` + `thread/goal/set` + `thread/goal/clear`，新增 `scripts/rescue-empty-preview.mjs`；round-80：首条用户消息正文为空导致线程从列表消失——新增 `resolveTurnPromptText` 空正文兜底；round-79 回退后消息列表倒序修复。round-78 线程打开时延根治已随 v0.1.122 发布。round-84 有 2 个未推送提交（代码+测试 `1e6045d`、文档 `0a319e0`），round-85 的代码+测试与文档提交紧随其后，两轮的文档提交均在本批次内） |
| Dev 端口 | 4173 |
| Dev 状态 | **round-86（本轮）**：版本仍停 **0.1.124**；**真实 Linux（WSL2 + Ubuntu 20.04、Node v22.22.2）全量 Vitest 649 例：649 通过 / 0 失败**（68 文件）、`vue-tsc --noEmit` 干净；Windows 同套 649 例 **647 通过 / 2 失败**（`codexAppServerBridge.archive.test.ts` 的 symlink realpath、`mode 0o600` vs `0o666`）——**从 round-73 一路挂到现在的「2 例 Windows 环境性失败」注记本轮在真实 Linux 上关闭**；协议假设脚本 `scripts/probe-turn-page.cjs` **11/11 PASS**（元数据读等价、resume 页可串链且完整、`turnsBackwardsCursor` 重发自己那页、分页窗口 = 水合切片反转、turn id 不是游标）；**真实桥（`:4173`）同环境 `git stash` 端到端 A/B**（同一 dev server、同一线程、四锚点）：`boundary` **483/516ms**（基线 883/913ms）、`interior` 943/1391ms（基线 62ms\*）、`unknown` 8ms、无锚点 91ms，**四条路径响应 SHA256 与字节数全部逐字相同**；\*基线按线程缓存整线程水合 30s（首个 883ms、之后任意锚点 62ms），有界路径按 (锚点, limit) 缓存（每个新页各付约 350–500ms、不随历史增长）——**单步/少步上翻有界路径快约 1.8×，连续多步基线靠摊薄可能更省**，两条路都保留且回落会把基线缓存重新焐热；以下是历史口径（round-85）。**round-85**：版本仍停 **0.1.124**；全量 Vitest **623 通过 / 2 失败**（625 例；基线 617 例同 2 例失败，+8 为本轮新增 `sessionLogRecovery.test.ts`）、`vue-tsc --noEmit` 干净、判据脚本 `scripts/probe-session-log-recovery.cjs` 对两种形状均按预期报告（30.89MB 当前形状 → `DOES NOT APPLY`，cache miss **154.1ms**（read 92.9 + parse 61.2）→ cache hit **0.005ms**；0.88MB 旧形状 → `APPLIES`，识别 58 行）、**真实桥（`:4173`）同环境 `git stash` A/B**（同一线程、同一脚本、修复前后各两轮 × 3 次请求）热态 **1188/1011/889/1071/1280ms（中位 1071）→ 668/560/610/635/492ms（中位 610）**，**修复后最大值 668ms < 修复前最小值 889ms**、客户端可见载荷仍 **1.56MB**、`turns=10` / `threadTurnStartIndex=6` / 响应不含 `initialTurnsPage`、**10 轮条目顺序与「跳过合并」基线逐字一致**、旧形状两会话经桥仍注入 **58/58 与 51/51** 条 `session-cmd-`、live-state 路由 HTTP 200 / 16 轮正常、单测含缓存语义两条（同 size+mtime 换内容后不得再读、日志增长后必须重判）；以下是历史口径（round-84）。**round-84**：版本仍停 **0.1.124**；全量 Vitest **615 通过 / 2 失败**（617 例；基线 591 例同 2 例失败，+23 为本轮新增 `threadResumeTurnPage.test.ts`）、`vue-tsc --noEmit` 干净、协议假设脚本 `scripts/probe-resume-turn-page.cjs` **7/7 PASS**（同一 16 轮线程：全量水合 **1153ms / 12.12MB** → 有界页 **198ms / 4.29MB**）、**真实桥（`:4173`）端到端**：`POST /codex-api/rpc` 的 `thread/resume` → HTTP 200 / `thread.turns.length=10` / `threadTurnStartIndex=6` / 响应**不含** `initialTurnsPage` / 首尾轮次 id 与旧路径一致 / 客户端可见载荷仍 **1.56MB**（与改动前同量，说明变的是服务端成本而非客户端体积）、**同一桥上 A/B**（`excludeTurns:false` 即旧路径，同一份代码同一进程）交替三轮 **3167→1203ms / 2289→734ms / 2532→1242ms**（2.63× / 3.12× / 2.04×），三轮均 `turns=10 startIndex=6` **输出逐字一致**；以下是历史口径。版本 **0.1.124** 发布前验证（round-82）：全量 Vitest **589 通过 / 2 失败**（+2 为本轮新增 `httpServer.websocketShutdown.test.ts`；round-83 后为 **591 通过 / 2 失败**，+2 为客户端自动压缩 turn 边界预检用例（该轮改动尚未发版，版本仍停在 0.1.124 待发，详见 [round-83](rounds/round-83-auto-compact-turn-boundary.md)）；2 例仍为 `codexAppServerBridge.archive.test.ts` 的既有 Windows 环境性失败）、`vue-tsc --noEmit` 干净（`TSC_EXIT=0`）、tsup CLI 构建通过、真实 dist-cli 启动 + `GET /` 200 + `/codex-api/ws` 握手通过、产物 `dist-cli/index.js` 含 `client.terminate()` 与两个 `close*Connections`（**注意 `dist-cli/` 未入库，是构建产物；2026-09-15 复查时工作区那份仍是 v0.1.123 旧产物、不含本次修复，复核前须先 `tsup` 重建**）、**打包产物端到端 A/B（2026-09-15 补做，Windows）**：同一 bundle、2 个真实 ws 客户端下修复前 **exit 1 @ 5015ms** → 修复后 **exit 0 @ 11ms**（0 客户端对照 10ms），**2026-09-15 已在 WSL2 真实 Linux 补做 A/B**：同 bundle 下修复前 **exit 1 @ 5009ms** → 修复后 **exit 0 @ 7ms**（0 客户端 8ms），并在临时启用的 systemd 249 下用探针 unit 实测——pre-fix `Result=exit-code/1` + journal `Failed with result 'exit-code'.` 逐字复现，post-fix `Result=success/0`、`systemctl stop` 14ms、`restart` 40ms、无失败行，至此含 OS 信号投递与 systemd 日志在内的整条链路全部闭环；本轮修复前 0.1.123 的口径为全量 Vitest **587 通过 / 2 失败**（`codexAppServerBridge.archive.test.ts` 的既有 Windows 环境性失败）、`vue-tsc --noEmit` 干净、`vite build`（335 模块）与 `tsup` CLI 均通过；round-81 救济走**真实 dist-cli 服务**的 `/codex-api/rpc` 端到端验证（隔离 `CODEX_HOME` 造两条受害者：基线 `thread/list` 0 条 → `resume + goal/set + goal/clear` → 2 条可见、文案 `clip.mp4` / `[Attachment]`，服务重启后仍在；负面结果同样在案：直写 `state_*.sqlite` 的 `preview` 列对列表无效、未 resume 的 `goal/set` 只写缓存不落 rollout）；round-80 空正文兜底已以隔离 `CODEX_HOME` 回放真实 app-server 0.153.4 验证（只带附件的首消息 `preview` 非空、2/2 行可见）、定向 Vitest 7/7、全量 587 通过/2 既有 Windows 环境性失败、`vue-tsc` 与 `vite build`+`tsup` 通过，未操作 5173；dev server 已验证（round-78 用 codegraph 图谱复查后，以真实 home 环境做同口径 A/B：短路 provider-models 首次点击 1102→126ms，据此定位并修复；探针 `output/playwright/perceived-latency.cjs` 已扩展时间线与 `PROFILE_FAKE_GIT`/`PROFILE_FAKE_MODELS` 开关）；v0.1.122 以定向 Vitest（round-77 新增 3 例 + round-78 新增 6 例）+ 全量 Vitest 578 通过/2 既有 Windows 环境性失败 + `pnpm run build`（`vue-tsc --noEmit` + `vite build` + `tsup` CLI）通过验证；版本 0.1.122 已 tag/Release（`v0.1.122` 为 Latest）且 `codex-mobile-re@0.1.122` 已 publish 至 npm（`latest` = 0.1.122），发布链路闭环；不操作 5173 |
| App-server | 本机 Codex CLI `0.153.4` 已生成并验证 app-server schema（json 416 + typescript 827 文件） |
| 工具链 | Windows：pnpm 11.18.0 · Node 24.18.1（fnm）· codex-cli 0.149.1（pnpm 全局）；macOS：Node v26.3.1 · 需按实际环境确认 codex-cli 版本 |
| npm `latest` | `codex-mobile-re@0.1.123`（发布时刻 `2026-09-14T12:09:44.833Z`，shasum `0be567b2068556baa644f082270e72501a8d796e`；2026-09-15 复查）；**`0.1.124` 尚未 publish（用户 2026-09-15 决定暂缓）** |
| 最近发布（v0.1.124，round-82） | **systemd 停止超时**：`systemctl restart codexapp.service` 每次都留一条失败日志，形态 `Failed with result 'exit-code'`（应用兜底 `exit 1`）与 `Failed with result 'timeout'`（systemd SIGKILL）交替——两个 5 秒定时器赛跑（systemd `DefaultTimeoutStopSec=5s` vs 应用 5000ms 兜底），但病根是 `shutdown()` 里 `server.close()` 在 5 秒内不返回。**实测否定了「补 `closeIdleConnections()`/`closeAllConnections()` 即可」的思路**：真实 Node 22.22.2 + 真实 ws 探测显示 keep-alive 连接不持有（回调 1ms），**websocket 才是持有者，且两个 `close*Connections()` 对升级后的 socket 完全无效**（Node 在 `upgrade` 后把 socket 移出 tracked connections，两方法都遍历不到，而 `server._connections` 仍计 1），只有显式 `terminate()` 客户端能释放（2ms）。修复 2 文件：`src/server/httpServer.ts` 新增导出 `shutdownWebSocketServer(wss)` 且 `attachWebSocket` 改为返回清理函数；`src/cli/index.ts` 的 `shutdown()` 先关 ws → `server.close()` → `closeIdleConnections()` → 1s 后 `closeAllConnections()`，并把 5 秒兜底**提到最前注册**（清理步骤抛错也不至于挂到 systemd 停超时）、`closeIdleConnections` 加 `typeof` 防护（engines 声明 `>=18` 而该 API 是 18.2 引入）。验证：新增契约单测 2/2（含「有 ws 客户端时 `close()` 不回调」基线）、全量 Vitest **589 通过 / 2 失败**（+2 为本轮新增，2 例为既有 `codexAppServerBridge.archive.test.ts` Windows 环境性失败）、`vue-tsc --noEmit` 干净、tsup CLI 构建通过、真实 dist-cli 启动 + `GET /` 200 + `/codex-api/ws` 握手通过、产物含新逻辑（**`dist-cli/` 未入库**，是构建产物，复核前须先 `tsup` 重建）。**验证闭环（2026-09-15）**：Windows 无法投递真实 SIGTERM（`child.kill` 走 TerminateProcess、JS handler 不执行），故 Windows 侧用一次性探针在**运行中的真实进程内**调用同一个 `shutdown()` 做打包产物端到端 A/B——修复前 **exit 1 @ 5015ms**、修复后 **exit 0 @ 11ms**（2 个真实 ws 客户端）/ **10ms**（0 个）；**Linux 侧已用 WSL2 补齐**：真实 POSIX 信号下修复前 **exit 1 @ 5009ms** → 修复后 **exit 0 @ 7ms**，并在 WSL 里临时启用 systemd 249 以探针 unit 实测 `systemctl stop` / `restart`——pre-fix `Result=exit-code/ExecMainStatus=1` + journal `Failed with result 'exit-code'.`（与用户报告逐字一致），post-fix `Result=success/0`、stop **14ms**、restart **40ms**、**无任何失败行**（唯一信号行是 systemd 清理 cgroup 内残留 app-server 子进程的 SIGKILL，不影响 `Result`）。详见 [round-82](rounds/round-82-systemd-stop-websocket-shutdown.md)。 |
| 最近发布（v0.1.123） | round-79 + round-80 + round-81（同批发布）。**round-81 已消失线程的救济路径**：用户指出 round-80 的受害者出现在**线上环境**（本机那份 `CODEX_HOME` 里没有受害者）；为线上线程找救济时实测**两次推翻** round-80 的收尾结论——①直写 `state_*.sqlite` 的 `preview` 列对 `thread/list` **无效**（服务运行中改列列表不变，停机改完冷启动该行仍不可见），说明 **rollout 才是元数据事实源、sqlite 只是写穿缓存**；②协议里确有一条侧门：`thread/goal/set` 会往 **rollout** 追加 `thread_goal_updated` 事件，而元数据派生在首消息正文为空时回退到 goal objective；③关键前提是必须**先 `thread/resume`**（否则 RPC 返回 OK、缓存列也更新，但 rollout 什么都不写、列表不变、重启后又派生回空）。可用救济序列 = `thread/resume` → `thread/goal/set`（objective 填推导文案、status=complete）→ `thread/goal/clear`（实测清掉 goal 后 preview 仍保留、`goal/get` 为 null）。新增 `scripts/rescue-empty-preview.mjs`（默认 dry-run；读库只读；走运行中服务的 `/codex-api/rpc`，回环免鉴权；判据用 rollout 而非 `has_user_event`）；隔离 `CODEX_HOME` + **真实 dist-cli 服务**端到端验证：基线 `thread/list` **0 条 → 救济后 2 条可见**（`clip.mp4` / `[Attachment]`）、**服务重启仍在**。**round-80 首条用户消息正文为空导致线程从列表消失**：codex 0.153.4 列表查询 WHERE 含 `AND threads.preview <> `（部分索引同谓词），「附件前言 + 空正文」与「空文本块」两种首消息落库 `preview`/`title`/`first_user_message` 全为空串 → 整行被筛掉（3 行仅 1 行可见，纯文本对照正常）；修复为新增 `src/utils/turnPromptText.ts` 的 `resolveTurnPromptText`（正文 → 首个附件 label → `[Image]` → `[Attachment]`）并在 `startThreadTurn` / `buildQueuedTurnParams` 两处组装点接入，同口径回放 app-server 验证修复后 **2/2 行可见**。**round-79 回退后消息列表倒序修复**：全仓只有 `revertThread` 消费 `thread/turns/list`，实测 `sortDirection: 'desc'` 返回页 **newest-first**（UUIDv7 时间有序），而 `normalizeThreadMessagesV2` 保序递增 `turnIndex`——`revertThread` 未反转 desc 页直接消费，导致 paginated 回退后保留历史**整段倒序**且 `turnIndex` 全反向，silent 重载走 `mergeMessages(preserveMissing: true)` 不自愈；修复为 normalize 前 `[...page.data].reverse()`。验证：全量 Vitest **587 通过 / 2 失败**（既有 Windows 环境性失败）、`vue-tsc --noEmit` 干净、`vite build` + `tsup` CLI 通过。**发布闭环**：git tag `v0.1.123` 与 GitHub Release 已创建，`codex-mobile-re@0.1.123` 已由用户 publish 至 npm 官方源并成为 `latest`（发布时刻 `2026-09-14T12:09:44.833Z`，shasum `0be567b2068556baa644f082270e72501a8d796e`；2026-09-15 复查回填）。 |
| 上一轮发布（v0.1.122） | round-78：接入 `@colbymchenry/codegraph` MCP 并用图谱复查线程打开时延。**接入**：codegraph v1.5.0 已由 pnpm 全局安装、项目 `.codegraph/` 索引已存在；新增稳定启动器 `~/.workbuddy/bin/codegraph-mcp.cjs`（运行时在全局根下搜索 `npm-shim.js` 并委派，避免 pnpm 版本哈希目录在 upgrade 后失效），`~/.workbuddy/mcp.json` 合并追加 `codegraph`（`serve --mcp`）；握手验证 `serverInfo={name:codegraph,version:1.5.0}`、`tools/list=[codegraph_explore]`；MCP 需在连接器管理页「信任」后才加载，故本轮复查用等价的 CLI（`explore`/`callers`/`query`）。**复查结论**：稳态切换已快（点击 #1/#2 82–264ms），但**首次点击固定 ~1.1s，且与线程内容无关**（9 条消息的线程也慢）。时间线显示 `thread/resume` 本身仅 32ms，却等到点击后 **+1221ms** 才发出——正是 `provider-models` 结束（+1208ms）之后。**A/B 归因**：单独短路 `/codex-api/provider-models*` → 首次点击 **1102→126ms**；单独短路 `/codex-api/free-mode/status` → 1058ms（无变化）；短路全部 `/codex-api/git/**` → 1121→1092ms（无变化，故 git 不是瓶颈，更正 round-77 把 `git/branches` 500 归因于「git 不可用」的说法——git 可用，只是本机单条 git 命令就要 316–488ms、路由串行跑 4–6 条）。**根因**：`selectThread` 里 `await refreshModelPreferences({ includeProviderModels: true })` 把外部 provider 目录放在了打开线程的关键路径上，而服务端 `fetchOpenCodeZenModelIds()`/`fetchCustomEndpointModelIds()` **每次调用都打一次外部请求（无缓存）**，`getFreeModels()` 更是**只在成功时写缓存**（失败则每次重付网络）且**其 `fetch` 连超时都没有**。**改动（3 文件）**：①`src/server/bridge/models.ts` 加模块级目录缓存（TTL 10min／空结果 1min／in-flight 去重，「过期先发后用」）；②`src/server/freeMode.ts` 给 openrouter 目录请求加 1.5s 超时、失败也记账（返回值不变）、兜底值只压 60s；③`src/server/codexAppServerBridge.ts` 新增 `warmProviderModelCatalog()` 并在既有启动预热块调用。**实测**：`provider-models` 1276ms→**3ms**，首次点击 **1102→~112ms（−90%）**（等于理论下限 126ms），其余阶段持平。新增 2 个测试文件（6 例全过）；全量 Vitest **578 通过/2 失败**（既有 Windows 环境性失败）、`vue-tsc` 干净。手测见 `tests/thread-loading-state/provider-model-catalog-cache-server.md`。版本随 **v0.1.122** 发布（与 round-77 同批）。详见 [round-78](rounds/round-78-provider-catalog-cache-open-thread-path.md)。 |
| 同批发布（v0.1.122） | round-77：线程打开时延——**轮耗时镜像写放大消除 + provider-models 并发去重**。用户反馈 round-76 后「左侧线程加载快了一点但还是有点慢；消息列表感觉没有变化」。测量（真实 home，最重 rollout 4.03MB）：桥层 warm 下 `thread/list` 91ms、`thread/resume` 148–718ms/374KB、`thread/read` 重复 149→9ms（round-76 缓存命中）、首屏总 API 仅 116.6KB（瘦身有效），但**并发请求争抢**才是瓶颈。根因：`savePersistedTurnDurationMap` 每次保存都把**全部线程的每一轮**逐条 `PUT /codex-api/thread-turn-durations`，且在启动合并服务端存档后又被调用 → 每次开页把服务端已有数据原样回写（本机 **27 个 PUT**），每个 PUT 在桥接层都是「读整份 `codex-global-state.json` + 写回」，占满浏览器 ~6 条同源连接并串行占用服务端文件 I/O，拖慢并发的 `thread/list`/`thread/resume`。**A/B 决定性验证**：把 PUT 风暴短路后 `thread/resume` 平均 224→84ms、点击线程 455/258/243→246/114/123ms。两处改动：①`useDesktopStatePersistence.ts` 新增 `mirroredTurnDurations` 快照 + `seedMirroredTurnDurations`，`savePersistedTurnDurationMap` **只发增量**；`loadThreadTurnDurationsIfNeeded` 合并服务端存档后先种子化再保存（来自服务端的数据不回写，纯本地新增仍上行）。②`src/api/gateway/models.ts` 加 `providerModelsInflight`，同一 provider 的并发 `provider-models` 调用共享 in-flight Promise（首屏 ×4 → ×2）。实测稳态度：`thread/resume` **224→81ms（−64%）**、点击线程 #1/#2/#3 **−47%/−65%/−49%**、`thread-turn-durations` 请求 **28→1**、请求总数 85→53、boot→侧栏 403–476ms（持平）。新增 `useDesktopStatePersistence.turn-durations.test.ts`（3/3）；全量 Vitest **572 通过/2 失败**（既有 Windows 环境性失败）、`vue-tsc` 干净。手测见 `tests/thread-loading-state/turn-duration-mirror-sends-deltas.md`。版本随 **v0.1.122** 发布（与 round-78 同批）。详见 [round-77](rounds/round-77-turn-duration-mirror-and-provider-models-dedupe.md)。 |
| 前一轮发布（v0.1.121） | round-76：线程/消息列表加载性能优化。基线实测最重线程 `thread/read` 13.55MB（19 轮未裁剪）、前端打开 3041.8ms/4.29MB、首屏 `/codex-api` 共 4,448.4KB，其中 92% 是 UI 从不整段渲染的大文本（`commandExecution` 74%、`mcpToolCall.result` 18%）。四项桥层改动：①删前端从不读取的 `mcpToolCall.result`；②命令输出超 16KB 中段截断（留头 60%+尾 40%）+ sha1 落盘 + 新增 `GET /codex-api/command-output?ref=` 按需回读，前端加截断提示行与「查看完整输出」入口；③`thread/read` 结果缓存（TTL 20s/≤6 条/变更即失效，不缓存 `thread/resume`），重复读 1745ms → 72ms；④app-server 启动预热（实测只能部分重叠冷启动，价值在后续加载，已如实写进注释）。实测最重线程响应体 **4,391.5KB → 1,599.1KB（−63.6%）**、首屏总字节 **−54.8%**。新增 3 个测试文件（`payloadSlimming`/`threadReadCache`/`commandOutputRoute`）+ `v2.test.ts` 3 例；全量 569 通过/2 失败（既有 Windows 环境性失败）。版本 bump 至 0.1.121，已 tag `v0.1.121`、创建 GitHub Release（Latest）并完成 npm publish（`latest` = 0.1.121）|

---
## 文档结构

> 本文件是交接总入口（快照/概况/启动/未完成事项/注意事项）。各轮验收与需求记录按轮次拆分在 `rounds/`，公共主题拆分在 `sections/`，点击下方链接查看详情。

### 环境与主题
| 主题 | 文件 |
| --- | --- |
| 当前运行状态 / 本次会话解决的问题 | [sections/environment.md](sections/environment.md) |
| 已提交的改动（commit 清单） | [sections/commit-history.md](sections/commit-history.md) |
| Codex 功能补齐方案完成情况（P0/P1/P2） | [sections/parity-plan.md](sections/parity-plan.md) |
| Reasonix 消息列表全量移植方案 | [sections/reasonix-plan.md](sections/reasonix-plan.md) |
| macOS 侧跨平台回归 | [sections/macos-regression.md](sections/macos-regression.md) |
| 客户端自动压缩（发送前预检 + 暂存补发）方案与评估 | [sections/auto-compact-plan.md](sections/auto-compact-plan.md) |
| 热点领域模块化（useDesktopState/codexGateway/codexAppServerBridge 拆分）方案 | [sections/domain-modularization-plan.md](sections/domain-modularization-plan.md) |
| 交接文档写作规范（本文档的组织与写法约定） | [handover-writing-guide.md](handover-writing-guide.md) |

### 按轮次记录
| 轮次 | 文件 |
| --- | --- |
| 第三轮验收修复 + 第四轮修复（压缩状态 bug） | [rounds/round-03-04-fixes.md](rounds/round-03-04-fixes.md) |
| 待办需求 7 条（2026-08-05）+ TUI 对比 + 需求 6 决策 | [rounds/round-05-backlog.md](rounds/round-05-backlog.md) |
| 第三轮验收调整 6 条 | [rounds/round-06-adjustments.md](rounds/round-06-adjustments.md) |
| 第四轮反馈 3 条 | [rounds/round-07-feedback.md](rounds/round-07-feedback.md) |
| 第五轮反馈 8 条 | [rounds/round-08-feedback.md](rounds/round-08-feedback.md) |
| 第六轮交接需求 2 条 | [rounds/round-09-handover.md](rounds/round-09-handover.md) |
| 第七轮交接需求 9 条（含需求 9 调研） | [rounds/round-10-handover.md](rounds/round-10-handover.md) |
| 第八轮：上游 PR 移植 5 个 | [rounds/round-11-upstream.md](rounds/round-11-upstream.md) |
| 第八轮交接需求（requirement-8）14 项 | [rounds/round-12-req8.md](rounds/round-12-req8.md) |
| 第九轮交接需求 4 条 | [rounds/round-13-fixes.md](rounds/round-13-fixes.md) |
| 第十轮交接需求 3 条 | [rounds/round-14-fixes.md](rounds/round-14-fixes.md) |
| 第十一轮交接需求 7 个问题 | [rounds/round-15-fixes.md](rounds/round-15-fixes.md) |
| 第十二轮反馈 3 条 + 调研 | [rounds/round-16-feedback.md](rounds/round-16-feedback.md) |
| 第十三轮反馈 8 个问题 + 调研 | [rounds/round-17-feedback.md](rounds/round-17-feedback.md) |
| 第十四轮交接需求 8 项 | [rounds/round-18-handover.md](rounds/round-18-handover.md) |
| 第十五轮交接需求（ThreadConversation 拆分重构） | [rounds/round-19-refactor.md](rounds/round-19-refactor.md) |
| 遗留项补齐（键盘导航 + 中断回填 + 溢出修复） | [rounds/round-20-leftover.md](rounds/round-20-leftover.md) |
| 第十六轮交接需求 9 条 | [rounds/round-21-feedback.md](rounds/round-21-feedback.md) |
| 第十七轮样式强化（命令块/折叠条去卡片化） | [rounds/round-22-style.md](rounds/round-22-style.md) |
| 第十八轮交接需求 10 条（字体/操作条/overlay/plan popover/面板/思考时序与跨浏览器存档） | [rounds/round-23-feedback.md](rounds/round-23-feedback.md) |
| 第十九轮交接需求 7 条（用户操作条统一/思考块去卡片/overlay 冗余/fileChange 轮末/思考时序刷新/标题截断/工具块去卡片） | [rounds/round-24-feedback.md](rounds/round-24-feedback.md) |
| OpenCode Zen `reasoning_content` 往返修复（多工具调用 400） | [rounds/round-25-zen-reasoning-fix.md](rounds/round-25-zen-reasoning-fix.md) |
| 第二十轮反馈 7 条（overlay 冗余移除/思考时序锚点前缀/思考高度/思考图标/操作条统一/fileChange 独立 li/右侧面板暗色失效） | [rounds/round-26-feedback.md](rounds/round-26-feedback.md) |
| 第二十一轮反馈 12 条（plan 刷新持久化/右侧面板暗色迁移/message-card 宽度/思考快照恢复/按钮颜色图标/聊天块调研/输出边框/自动压缩调研/页面刷新调研/压缩双块/末轮思考时序） | [rounds/round-27-feedback.md](rounds/round-27-feedback.md) |
| 第二十二轮反馈 4 条（plan 执行中刷新按钮可执行/计划完成按钮可点击/进行中触发自动压缩调研/中文输入法 Enter 提前发送） | [rounds/round-28-feedback.md](rounds/round-28-feedback.md) |
| 第二十三轮反馈 1 条（刷新后思考块堆在模型回答开头——live 锚点 id 被 app-server 恢复改写导致失配，锚点失配按无锚点分摊修复） | [rounds/round-29-feedback.md](rounds/round-29-feedback.md) |
| 第二十四轮反馈 3 条（计划面板 implemented 判定：刷新加载中按钮误可点 + 单轮长任务同轮工作项不识别；压缩块刷新后归位到轮首用户消息之后） | [rounds/round-30-feedback.md](rounds/round-30-feedback.md) |
| 第二十五轮：侧边栏过滤 subagent 会话（上游 v514 对比 + app-server 物化实测 + 桥接层 thread_source 过滤） | [rounds/round-31-subagent-filter.md](rounds/round-31-subagent-filter.md) |
| 第二十六轮反馈 3 条（fileChange 折叠样式损坏；工具调用块跑到对话最后——session- 前缀幂等保护失效 + 物化合并回复；fileChange 撤销按单个文件） | [rounds/round-32-feedback.md](rounds/round-32-feedback.md) |
| 第二十七轮反馈 1 条（fileChange 块样式与消息流其他块风格不一致——round-17 去卡片化后的残留）+ README fork 身份重写 + codex-cli 0.147.0 升级 | [rounds/round-33-feedback.md](rounds/round-33-feedback.md) |
| 第二十八轮反馈 2 条（processFold 工具调用块全跑到对话前面——空文本 assistant 虚高 agent slot 数；fileChange 每行变更数字/撤销按钮移到最左边） | [rounds/round-34-feedback.md](rounds/round-34-feedback.md) |
| 第二十九轮反馈 2 条（fileChange 每行变更数字/撤销按钮移到最右边——round-34 方向相反；文件名过长未省略需截断） | [rounds/round-35-feedback.md](rounds/round-35-feedback.md) |
| 第三十轮反馈 1 条（回退成功后再次发送，被回退的消息复现——回退按钮残留上游「编辑」流程的输入框回填） | [rounds/round-36-feedback.md](rounds/round-36-feedback.md) |
| 第三十一轮 3 项（回收站线程名丢失、文件面板树形结构、消息列表图片/视频预览） | [rounds/round-37-feedback.md](rounds/round-37-feedback.md) |
| 第三十二轮 3 项（@ 文件提及混入 .git 内部文件、收发图片/视频渲染确认可用） | [rounds/round-38-feedback.md](rounds/round-38-feedback.md) |
| 第三十三轮 3 项（新线程发图排查=提供方限流、@main 无 rg 兜底修复、孤儿思考块堆末尾修复） | [rounds/round-39-feedback.md](rounds/round-39-feedback.md) |
| 第三十四轮 1 项（发送图片模型无法理解——zen-proxy 丢弃 input_image 修复） | [rounds/round-40-feedback.md](rounds/round-40-feedback.md) |
| 第三十五轮 1 项（自定义端点"没用"——URL 路径重复 + model 空修复） | [rounds/round-41-feedback.md](rounds/round-41-feedback.md) |
| 第三十六轮 2 项（回退后消息回填输入框、选 codex 用 codex-cli 同款 litellm 模型） | [rounds/round-42-feedback.md](rounds/round-42-feedback.md) |
| 第三十七轮 2 项（侧边栏泄漏子 agent 线程——subagent rollout 用自身 id 键控；模型强度下拉收敛到 Low/Medium/High） | [rounds/round-43-feedback.md](rounds/round-43-feedback.md) |
| 第三十八轮 1 项（WebUI 预览后 TUI 无法恢复线程——writer 锁已知限制，纯诊断+文档化，无代码改动） | [rounds/round-44-feedback.md](rounds/round-44-feedback.md) |
| 第三十九轮 1 项（最后一轮思考跑到模型回答最后——thread/read 时序恢复把 reasoning 挤到轮末，改为紧贴其消息） | [rounds/round-45-feedback.md](rounds/round-45-feedback.md) |
| 第四十轮 4 项 H5/UI 打磨（diff 弹窗 Teleport 到 body + z-[1200] 修复被顶栏遮挡/无法关闭、模型按钮 w-fit+max-w、命令块 header 改「序号+命令+状态」并移命令入展开输出区） | [rounds/round-46-feedback.md](rounds/round-46-feedback.md) |
| 第四十一轮 Hot 区消息按 turn 结构重构（真实 request/process/final 容器与过程视觉层级） | [rounds/round-47-refactor.md](rounds/round-47-refactor.md) |
| 第四十二轮 前端大文件组件化（侧栏线程行复用 + Directory Hub 标签页拆分，方案见 `docs/componentization-plan.md`） | [rounds/round-48-componentization.md](rounds/round-48-componentization.md) |
| 第四十九轮 前端大文件组件化系列完成（Sidebar 线程行 / Directory Tab / App 设置账号面板 / Composer+Conversation 展示层，含验证与手测文档） | [rounds/round-49-componentization.md](rounds/round-49-componentization.md) |
| 第五十轮 侧边栏重新出现子 agent 会话（tracker 竞态 + 前端并集合并残留修复） | [rounds/round-50-subagent-race.md](rounds/round-50-subagent-race.md) |
| 第五十一轮 最新一轮缺失 assistant 块 + Zen Proxy 多 agent namespace 工具丢失（agent 消息完成态 + namespace 展开/还原） | [rounds/round-51-final-assistant-and-zen-namespace.md](rounds/round-51-final-assistant-and-zen-namespace.md) |
| 第五十二轮 多 agent 进行中重复 agentMessage 块 + message-toolbar 闪现（live 文本级去重修复，`upsertLiveAgentMessage` 复用 `normalizeMessageText`；含补充修复：`buildTurnRenderGroups` 增 `liveOverlayActive` 双守卫，防止 live 流式中已完成中间消息被误提升为 final） | [rounds/round-52-duplicate-agent-live.md](rounds/round-52-duplicate-agent-live.md) |
| 第五十三轮 live agent turn 所有权 + 后台标签恢复卡顿（上一轮迟到 agent 消息按 `turnId` 归位；`thread/list` 使用 tracker 缓存且前台恢复不等待附属元数据） | [rounds/round-53-live-turn-ownership-and-resume.md](rounds/round-53-live-turn-ownership-and-resume.md) |
| 第五十四轮 Codex `0.149.1` 协议快照与通知兼容回归（4 组通知测试；无运行时代码改动） | [rounds/round-54-codex-0.149.1-protocol-compatibility.md](rounds/round-54-codex-0.149.1-protocol-compatibility.md) |
| 第五十五轮 Vite 根路径被 SSE 中间件阻断（精确事件路由 + 非 API 请求提前放行） | [rounds/round-55-vite-sse-root-route.md](rounds/round-55-vite-sse-root-route.md) |
| 第五十六轮 v0.1.105 发布（服务观察验证 + 版本发布） | [rounds/round-56-v0.1.105-release.md](rounds/round-56-v0.1.105-release.md) |
| 第五十七轮 过程收尾记录遮蔽最终总结 | [rounds/round-57-final-summary-after-process.md](rounds/round-57-final-summary-after-process.md) |
| 第五十八轮 完成后最终总结回落到过程区 | [rounds/round-58-completed-live-summary.md](rounds/round-58-completed-live-summary.md) |
| 第五十九轮 v0.1.106 发布（最终总结归属修复） | [rounds/round-59-v0.1.106-release.md](rounds/round-59-v0.1.106-release.md) |
| 第六十轮 桌面前后台恢复同步修复 | [rounds/round-60-desktop-foreground-resume-sync.md](rounds/round-60-desktop-foreground-resume-sync.md) |
| 第六十一轮 v0.1.107 线程状态修复（异步子代理过滤快照、未知 liveTurnId 的历史最终总结） | [rounds/round-61-v0.1.107-thread-state-fixes.md](rounds/round-61-v0.1.107-thread-state-fixes.md) |
| 第六十二轮 前端领域模块化续期（Preferences、Settings、Rate limits、Projects、Catalogs、Queue） | [rounds/round-62-domain-modularization.md](rounds/round-62-domain-modularization.md) |
| 第六十三轮 ThreadConversation / App.vue hook 化与 v0.1.108 发布（8 个 ThreadConversation hook + `useSidebarUi`/`useRightPanel` + 每轮耗时 + props TDZ + 375px 回归） | [rounds/round-63-v0.1.108-hooks-and-release.md](rounds/round-63-v0.1.108-hooks-and-release.md) |
| 第六十四轮 v0.1.109 发布（回收站固定高度 + 思考强度默认 medium + ComposerPopover H5 附加菜单可见性修复） | [rounds/round-64-v0.1.109-recycle-bin-thinking-h5.md](rounds/round-64-v0.1.109-recycle-bin-thinking-h5.md) |
| 第六十五轮 v0.1.110 发布（每轮耗时胶囊徽标 + 服务端 sidecar 持久化、命令块闪烁修复） | [rounds/round-65-v0.1.110-turn-duration-persist-command-flash.md](rounds/round-65-v0.1.110-turn-duration-persist-command-flash.md) |
| 第六十六轮 v0.1.111 发布（回退保留目标轮次 + 解析 function_call apply_patch + 现有线程乐观 UI） | [rounds/round-66-v0.1.111-rollback-optimistic-ui.md](rounds/round-66-v0.1.111-rollback-optimistic-ui.md) |
| 第六十七轮 v0.1.112 发布（线程切换性能优化 + 回退最后一条消息修复） | [rounds/round-67-v0.1.112-thread-switch-perf-rollback-last.md](rounds/round-67-v0.1.112-thread-switch-perf-rollback-last.md) |
| 第六十八轮 v0.1.113 发布（回退移除目标轮及其后所有轮次） | [rounds/round-68-v0.1.113-rollback-target-turn.md](rounds/round-68-v0.1.113-rollback-target-turn.md) |
| 第六十九轮 v0.1.114 发布（回退不存在静默 no-op，真实 app-server rollback 语义确认） | [rounds/round-69-v0.1.114-rollback-no-silent-nop.md](rounds/round-69-v0.1.114-rollback-no-silent-nop.md) |
| 第七十轮 v0.1.115 发布（修复空的 processFold 块） | [rounds/round-70-fix-empty-processfold-block.md](rounds/round-70-fix-empty-processfold-block.md) |
| 第七十一轮 v0.1.116 发布（修复空的 agentMessage/通用正文过程行） | [rounds/round-71-fix-empty-process-row.md](rounds/round-71-fix-empty-process-row.md) |
| 第七十二轮 v0.1.117 发布（修复线程模型切换被 resume 旧模型覆盖） | [rounds/round-72-fix-thread-model-switch-on-resume.md](rounds/round-72-fix-thread-model-switch-on-resume.md) |
| 第七十三轮 v0.1.118（切换模型后上下文窗口失效 + 模型切换分割栏） | [rounds/round-73-model-switch-context-and-divider.md](rounds/round-73-model-switch-context-and-divider.md) |
| 第七十四轮 v0.1.119（回退 paginated 历史迁移补完：按 `historyMode` 一次分流消除探路 502 + `revertThread` 游标增量 hydrate 消除列表刷新 + 桥接裁剪补全 `thread/revert`） | [rounds/round-74-rollback-paginated-fixes.md](rounds/round-74-rollback-paginated-fixes.md) |
| 第七十五轮 v0.1.120（侧边栏重复线程行去重：paginated rollout 分段同一 id 多行在 bridge 层按 id 归并保留最新段 + 前端渲染源 `dedupeThreadGroupsById` 兜底） | [rounds/round-75-dedupe-paginated-thread-segments.md](rounds/round-75-dedupe-paginated-thread-segments.md) |
| 第七十六轮 v0.1.121（线程/消息列表加载性能优化：删未消费的 `mcpToolCall.result` + 命令输出 16KB 内联截断/sha1 落盘/按需回读 + `thread/read` 结果缓存 + app-server 启动预热；最重线程响应体 −63.6%、首屏 API 字节 −54.8%、重复读 −95.9%） | [rounds/round-76-thread-loading-perf.md](rounds/round-76-thread-loading-perf.md) |
| 第七十七轮 v0.1.122（线程打开时延：轮耗时镜像写放大消除——只发增量、启动不回写服务端已有数据；provider-models 并发去重；`thread/resume` 224→81ms、点击线程 −47%~−65%、`thread-turn-durations` 请求 28→1） | [rounds/round-77-turn-duration-mirror-and-provider-models-dedupe.md](rounds/round-77-turn-duration-mirror-and-provider-models-dedupe.md) |
| 第七十八轮 v0.1.122（接入 codegraph MCP + 用图谱复查：定位「首次点击线程固定 ~1.1s」= `selectThread` 在关键路径上 await 外部 provider 目录，而服务端 zen/custom 目录请求与 openrouter 目录请求都缺少缓存/超时；加目录缓存 + 失败记忆化 + 1.5s 超时 + 启动预热后首次点击 **1102→~112ms**、`provider-models` 1276→3ms） | [rounds/round-78-provider-catalog-cache-open-thread-path.md](rounds/round-78-provider-catalog-cache-open-thread-path.md) |
| 第七十九轮 v0.1.123 发布（回退后消息列表倒序渲染修复：`thread/turns/list` desc 页实测 newest-first，`revertThread` 未反转直接消费导致 paginated 回退后保留历史整段倒序、`turnIndex` 全反向，且 silent 重载 preserveMissing 不自愈；normalize 前反转页序修复） | [rounds/round-79-rollback-revert-order.md](rounds/round-79-rollback-revert-order.md) |
| 第八十轮 v0.1.123 发布（首条用户消息正文为空导致线程从列表消失：只发附件/图片而无正文时 app-server 派生的 `preview`/`title`/`first_user_message` 全为空串，`thread/list` 的 `preview <> ''` 谓词把整行筛掉、无元数据 RPC 可修回（round-81 更正：另有 `resume + goal/set` 侧门可救济）；新增 `resolveTurnPromptText` 空正文兜底（首个附件名 → `[Image]` → `[Attachment]`）并在两处组装点接入，隔离 `CODEX_HOME` 回放 app-server 0.153.4 验证修复后 2/2 行可见） | [rounds/round-80-attachment-only-first-message-thread-visibility.md](rounds/round-80-attachment-only-first-message-thread-visibility.md) |
| 第八十一轮 v0.1.123 发布（已消失线程的救济路径：round-80 的「事后修不回来」被证伪——直写 `state_*.sqlite` 的 `preview` 列对 `thread/list` 无效，rollout 才是元数据事实源；可用路径是 `thread/resume`（必须，先把线程加载起来）+ `thread/goal/set`（goal 事件落 rollout，派生时回退到 objective）+ `thread/goal/clear`（清掉 goal、preview 保留）；新增 `scripts/rescue-empty-preview.mjs`，隔离 `CODEX_HOME` + 真实 dist-cli 服务端到端验证 0→2 条可见、重启仍在） | [rounds/round-81-vanished-thread-rescue.md](rounds/round-81-vanished-thread-rescue.md) |
| 第八十二轮 v0.1.124 发布（systemd 停止超时：每次 `systemctl restart` 都留失败日志，`exit-code`（应用 5s 兜底 `exit 1`）与 `timeout`（systemd SIGKILL）交替；实测 websocket 升级后的 socket 脱离 http server 的 tracked connections，`closeIdleConnections()`/`closeAllConnections()` 都够不到它（keep-alive 实测 1ms 不持有），必须显式 `terminate()` 客户端；新增 `shutdownWebSocketServer`、`attachWebSocket` 返回清理函数，`shutdown()` 补齐连接清理并把 5s 兜底提前注册；并在 WSL2 真实 Linux + 临时启用的 systemd 249 下复测——pre-fix `Failed with result 'exit-code'.` 复现、post-fix 无失败行） | [rounds/round-82-systemd-stop-websocket-shutdown.md](rounds/round-82-systemd-stop-websocket-shutdown.md) |
| 第八十三轮 round-83（客户端自动压缩在长 turn 下不触发——阈值此前只被「发送 + 空闲」消费，turn 内按设计跳过、turn 结束又只补发暂存不做压缩，改为在 turn 转空闲那一刻按同一阈值预检；实测服务端本就在 turn 内 ~90% 处自己压缩，客户端默认阈值 10% 剩余与之同点，本轮按用户决定维持 10% 且不 bump 版本） | [rounds/round-83-auto-compact-turn-boundary.md](rounds/round-83-auto-compact-turn-boundary.md) |
| 第八十四轮 round-84（打开会话去掉全量历史水合：用户口径「慢的不是 DOM，等待全在 RPC」；`thread/resume` 让 app-server 全量水合后再被桥裁到 10 轮，实测 2118ms/12.12MB 全部白付；改为请求 `{excludeTurns, initialTurnsPage{10,desc,full}}`（协议推荐、全量水合已标 deprecated），桥把返回页反转成升序提升为 `thread.turns` 并补 `threadTurnStartIndex`，同桥上 A/B **2.0–3.1×**、输出逐字一致、前端零改动；旧 app-server 忽略 `initialTurnsPage` 时按原请求重放兜底；新增 `scripts/probe-resume-turn-page.cjs` 固化协议假设） | [rounds/round-84-open-thread-bounded-hydration.md](rounds/round-84-open-thread-bounded-hydration.md) |
| 第八十五轮 round-85（会话日志时序恢复的适用范围：round-84 后 app-server 只剩 198ms、经桥仍 734–1242ms，逐段量出最大一段是命令合并 **434–681ms**（读 167–295 + 解析 220–348），而 CLI 已把命令从 `function_call exec_command` 换成 `custom_tool_call exec`，当前形状日志**识别到 0 行**（最新两个会话 0 识别 / 222+7 条新形状）——没有命令槽时它仍把 user/agent 提前、命令追加轮末，反向破坏 app-server 已正确的交错；改为「形状闸门（无 `commandExecution`/`fileChange` 槽即原样返回、全无改动返回入参同一引用）+ 版本判据缓存（`mergeSessionCommandsIntoTurnsFromPath`：判据为假只付一次 stat 不读文件、size/mtime 变则重判）」，同环境 `git stash` A/B 热态中位 **1071→610ms（−43%）**、条目顺序与「跳过合并」基线 10/10 逐字一致、旧形状两会话仍 58/58 与 51/51、新增 `scripts/probe-session-log-recovery.cjs` 固化判据） | [rounds/round-85-session-log-recovery-scope.md](rounds/round-85-session-log-recovery-scope.md) |
| 第八十六轮 round-86（**上翻更早轮次改为游标分页**：`readThreadForTurnPage` 仍发 `thread/read {includeTurns:true}` 再内存 slice，实测 **1295ms / 12.12MB** 而 `includeTurns:false` 只要 **2ms / 0.00MB**；**并纠正 round-84/85 的错误措辞**——顶层 `turnsBackwardsCursor` `includeAnchor:true`、用它只会**重发自己那一页**，真正往更老走的是 `initialTurnsPage.nextCursor`，且游标是不透明 JSON、**turn id 不是游标**，故**没有一次性「取某轮之前一页」的调用、只能逐页串链**；设计为「升序 id 列（`turns/list{10000,notLoaded,desc}` 2–14ms/0MB，不缓存）+ `ThreadTurnPageCursorChain`（threadId→(turnId→更老游标)，64×256 LRU，来源=打开会话那页 + 本路由服务过的每页）+ 页长/页首页尾校验」，**任何意外返回 null 回落原全量水合、回落路径逐字相同**；元数据等价性先验（`includeTurns:false` 的 `thread` 32 键全同、仅 `turns` 空）；新增单测 24 例、`scripts/probe-turn-page.cjs` **11/11 PASS**、`git stash` 端到端 A/B **四锚点响应逐字相同（含字节数）**、**真实 Linux（WSL2）全量 649/649 通过**（关闭既有的 2 例 Windows 环境性失败注记）、`SHARED_BRIDGE_VERSION` v3→v4） | [rounds/round-86-bounded-older-turn-paging.md](rounds/round-86-bounded-older-turn-paging.md) |

## 项目概况

`codexapp` 是一个面向 Codex 的轻量级 Web 界面，运行在 Codex app-server 之上，可从任何浏览器远程访问。技术栈为 Vue 3 + Vite 6 + TypeScript，**npm 包名 `codex-mobile-re`**（2026-08-10 起以本 fork 名发布，`codexapp`/`codexui` 为上游包名无发布权限）。上游仓库为 friuns2/codexUI（上游 npm 包 `codexapp`/`codexui` 归 friuns 所有），本机 fork 自 `<用户名>/codex-mobile`。

- 开发入口：`scripts/dev.cjs`，内部包装 Vite dev server
- 关键桥接层：`src/server/codexAppServerBridge.ts`（Vite 中间件，代理 `/codex-api/*` 到 codex app-server）
- 命令解析：`src/commandResolution.ts`（定位本机 codex CLI 可执行文件）
- 构建：`pnpm run build`（前端 vite build + CLI tsup）

## 启动方式

### 普通电脑（无沙箱限制）

```bash
# 首次安装依赖后即可直接启动
pnpm run dev --host 127.0.0.1 --port 4173
```

无需设置 `CODEX_HOME`，codex CLI 正常读写 `~/.codex`。

### TRAE 沙箱内

```powershell
# PATH 必须含两段：node 安装目录 + codex CLI 所在目录（位置按本机实际安装定位，见下方说明）
$env:PATH = '<node 安装目录>;<pnpm 全局 bin 目录>;' + $env:PATH
$env:CODEX_HOME='<项目目录>\.codex'
pnpm run dev --host 127.0.0.1 --port 4173
```

`CODEX_HOME` 必须指向沙箱允许写入的位置；项目内 `.codex/` 已被 `.gitignore` 忽略，不会污染 git。

> **重要（2026-08-06 实测教训）**：`resolveCodexCommand()` 通过 PATH 里的 `codex.CMD` shim 定位 codex CLI。codex-cli 由 pnpm 全局安装在 `<pnpm 全局 bin 目录>`（Windows 下 shim 为 `codex.CMD`），该目录通常不在默认 PATH 中。若只补 node 目录而漏掉 pnpm bin 目录，页面会报 `Codex CLI not found. Install @openai/codex or set CODEXUI_CODEX_COMMAND.`（`/codex-api/rpc` 502）。把 pnpm 全局 bin 目录前置进 PATH 后正常。定位方式：node 安装目录见 `Get-Command node` 或版本管理器（fnm/nvm）配置；pnpm 全局 bin 目录见 `pnpm bin` 或 `npm prefix -g`（corepack 安装时在 corepack 缓存目录内）。

### macOS（2026-08-07 实测）

```bash
# 0) 首次需全局安装 codex CLI（macOS 默认没有；npm registry 已配国内镜像，无需代理）
npm install -g @openai/codex
# 1) npm 全局 bin 不在默认 PATH 时前置进 PATH
export PATH="<npm 全局 bin 目录>:$PATH"
# 2) TRAE 沙箱模式：CODEX_HOME 指向项目内 .codex/（已被 .gitignore 忽略）
export CODEX_HOME='<项目目录>/.codex'
# 3) 启动
pnpm run dev --host 127.0.0.1 --port 4173
```

macOS 特有差异：`resolveCodexCommand()` 非 Windows 分支按 `codex`（PATH）→ npm 全局 `@openai/codex` 包顺序定位，装上即用，无 `.cmd` shim 问题；`node-pty` postinstall 在 macOS 跳过（由 `scripts/fix-pty-native-build.cjs` 处理），不影响终端面板。GitHub 直连超时时经本机 FlClash 代理（`socks5h://127.0.0.1:7890`）推送。

### 端口冲突

本机 `5173` 被 HBuilderX uniapp 占用，始终使用 `--port 4173` 规避。其他电脑若无冲突可自由选端口。

## 未完成事项

- **round-86 上翻更早轮次改为游标分页（去掉最后一处全量水合）（2026-09-16，未推送、未发版）**：用户口径「继续推进，你可以使用 wsl 进行回归验证」。round-84 修掉「打开会话全量水合」、round-85 修掉管道里白跑的会话日志恢复之后，剩**最后一处全量水合**——`readThreadForTurnPage`（上翻更早轮次用的 `/codex-api/thread-turn-page`）仍发 `thread/read {includeTurns:true}` 再在内存 `slice(-limit)`。**现象量化**（绕开桥、直连 app-server，16 轮 / 30.89MB rollout）：`thread/read {includeTurns:true}` = **1295ms / 12.12MB**、`{includeTurns:false}` = **2ms / 0.00MB**、`turns/list{10000,notLoaded,desc}` = 2–14ms / 0.00MB、`turns/list{cursor,desc,full}`（要的那 6 轮）= 338–354ms / 7.83MB——成本 100% 在构造轮次，裁剪发生在构造完成之后（与 round-84 同病）。**本轮先纠正 round-84/85 的错误技术措辞**：两轮遗留都写「顶层 `turnsBackwardsCursor` 正是它该用的游标」，**实测是错的**——①它带 `includeAnchor:true`，用它 `desc` 只会**原样重发它自己那一页**（拿它上翻原地打转）；②真正往更老走的是 **`initialTurnsPage.nextCursor`**（`includeAnchor:false`，响应写「continue after the last turn」）；③游标**只能逐页串链**（从 `nextCursor` 一路走恰好覆盖全部 16 轮、每轮一次、无重叠，顺序等于全量水合逆序）；④游标是**不透明 JSON**（`{"requestedThreadId":…,"rolloutOrdinal":2186,"includeAnchor":false,"scope":{"kind":"turns"}}`），**把 turn id 当 `cursor` 会被拒**（`{"code":-32600,"message":"invalid cursor: <turn-id>"}`）。推论：**没有「给我某一轮之前那一页」的一次性调用**。**元数据等价性先验**：`thread/read {includeTurns:false}` 返回的 `thread` 对象 **32 个键完全相同、除 `turns` 外值逐字相同**（也返回空 `turns`）、顶层 `result` 仍只有 `thread`——所以只换 `thread.turns` 就与改动前逐字一致，前端 `getOlderThreadMessagesV2` / `normalizeThreadMessagesV2` / `readThreadInProgressFromResponse` / `buildTurnIndexByTurnId` 与 `sanitizeThreadSkills`·命令合并管线一行未动。**设计**（新增 `src/server/bridge/threadTurnPage.ts`）：①`readThreadTurnIds` 发 `turns/list {10000, notLoaded, desc}`（2–14ms / 0.00MB，必要时按 `nextCursor` 翻页上限 50 页）反转成**升序 id 列**，一次回答锚点绝对下标 / `startTurnIndex` / `hasMoreOlder` / 锚点是否存在（**故意不缓存**，每轮拿最新事实）；②`ThreadTurnPageCursorChain` 按 `threadId → (turnId → 更老游标)` 记录页边界（64 线程 × 每线程 256 条，LRU），边界两个来源=「打开会话那页」（`resumeThreadWithTurnPage` 新增 `onTurnPageBoundary` 回调交出「最老轮」+ `initialTurnsPage.nextCursor`，**不是** `turnsBackwardsCursor`）+「本路由服务过的每页」；③命中边界发 `turns/list {cursor, desc, limit, itemsView:'full'}` 反转成升序，**必须校验**「页长 == 请求窗口」且「页首 == `ids[startTurnIndex]`、页尾 == `ids[beforeIndex-1]`」——游标会活得比它指向的轮次更久（revert / rollback / app-server 重启重编号），校验让陈旧游标只换一次回落而非**一页错误的对话**；基于同一理由游标链**不挂任何失效钩子**（每轮重列 id + 校验比到处清缓存更可靠）。**任何意外一律返回 null，由调用方走原来的全量水合**（id 列读不到 / 锚点不存在（返回空页，与旧行为一致）/ 没有边界游标 / 页长不符 / 页内容对不上 / `thread/read` 元数据失败），**回落路径与改动前逐字相同**。**接线**：`threadRoutes` 的 `/codex-api/thread-turn-page` 先试有界路径、再回落，尾部三段合并（`slimThreadTurnsPayload` / `mergeSessionSkillInputsIntoThreadResult` / `mergeSessionCommandsIntoThreadResult`）**统一**（三者都在 `turns.length === 0` 时原样返回入参，故空页走不走合并结果一致，不再需要为「锚点不存在」单独提前 return）；`codexAppServerBridge` 新增 `readBoundedThreadTurnPage` / `recordThreadTurnPageBoundary`、按 (线程, 锚点, limit) 缓存已组装页（TTL 30s、与既有 turn-page 缓存同一批失效点），`SHARED_BRIDGE_VERSION` **v3 → v4**。**验证**：①新增单测 **24 例**（`threadTurnPage.test.ts` 21 + `threadResumeTurnPage.test.ts` +3）；②协议假设固化为 `scripts/probe-turn-page.cjs <threadId> [CODEX_HOME] [codexBinPath] [limit]`，**11 项断言全过**（metadata read 是「空 turns 的水合线程」/ resume 页可串链且完整 / `turnsBackwardsCursor` 重发自己那页 / 分页窗口 = 水合切片反转 / turn id 不是游标），**app-server 升级后必跑**（失败则桥静默回落，症状是变慢不是出错，脚本是唯一能提前告知的东西）；③`git stash` 端到端 A/B（同一 dev server、同一线程、四锚点）**四条路径响应 SHA256 与字节数全部逐字相同**（`boundary` 483/516ms vs 基线 883/913ms、`interior` 943/1391ms vs 基线 62ms\*、`unknown` 8ms vs 1ms、无锚点 91ms vs 59ms）——\*基线按线程缓存整线程水合 30s，首个贵、之后任意锚点免费，有界路径按 (锚点, limit) 缓存每个新页各付约 350–500ms 且不随历史增长，故**单步/少步上翻有界快约 1.8×、连续多步基线靠摊薄可能更省**，两条路都保留（回落即基线，且回落会把基线按线程缓存重新焐热）；④**真实 Linux 全量回归（WSL2 + Ubuntu 20.04、Node v22.22.2）**：**649 例 649 通过 / 0 失败**（68 文件）、`vue-tsc` 干净——**从 round-73 一路挂到现在的「2 例 Windows 环境性失败」注记本轮在真实 Linux 上关闭**（同套用例同份代码 Linux 全绿，证明确为平台差异）；Windows 侧同套 649 例 647 通过 / 2 失败（同一 2 例）。Linux 侧还**当场抓到一个 Windows 漏掉的真实类型错误**（`threadTurnPage.test.ts` 假 rpc 类型不符，`vue-tsc` 报 TS2345）——Windows 那次 `vue-tsc` 跑在测试文件写完之前，第二平台的价值当场兑现。**Linux 环境怎么搭的**（WSL 无出网、无 Node，一次性）：Windows 侧 `tmp/fetch-linux-runtime.cjs` 取回 `node-v22.22.2-linux-x64.tar.xz`、`@esbuild/linux-x64@{0.25.0,0.27.7}`、`@rollup/rollup-linux-x64-gnu@4.62.4`，WSL 里把 Node 解到 `$HOME/linux-node`、平台包按 **pnpm 虚拟 store 布局**解到各自 `node_modules/.pnpm/<pkg>@<ver>/node_modules/...`（项目用 pnpm，配置加载器用嵌套 esbuild **0.25.0**、vite 用 **0.27.7**，两个都要补，只补根上一个会报 `Host version "0.25.0" does not match binary version "0.27.7"`）；运行入口 `tmp/wsl-vitest.sh [test|types]`。**遗留与后续**：①`threadArchiveRecovery` 的辅助 resume 仍可改 `excludeTurns:true`（round-84 遗留，未动，只在罕见救济路径）；②**非边界锚点会回落全量水合（by design）**——前端 `loadOlderMessages` 总拿「当前最老那一轮」当锚点、每答一页就登记边界，故顺序上翻恒有界，只有跳着要某一轮（如从搜索定位到深处）才回落，要覆盖需「从最新逐页走到锚点」的链式遍历，本轮未做；③桥侧管道成本仍是打开会话的主要开销（round-85 遗留）；④版本号仍停 **0.1.124**（npm publish 由用户执行）。详见 [round-86](rounds/round-86-bounded-older-turn-paging.md)。

- **round-85 会话日志时序恢复的适用范围（2026-09-16，未推送、未发版）**：接 round-84 的遗留项「桥侧管道的成本现在浮上来了（app-server 198ms vs 经桥 734–1242ms）」。**先在 `rpcPipeline` 八步管道上逐段打断点**（env 门控，测完撤除）：热态 `trim`/`turnErrors` 0.1ms、`sanitize` 46–76ms、`mergeSessionSkillInputs` 2–6ms（它有 size+mtime 缓存，冷启动 304ms）、**`mergeSessionCommands` 434–681ms**（这一支**从来没有缓存**），再细分得 **`readFile` 167–295ms + 解析并合并 220–348ms**——一次 30.89MB 日志的整读整解。**根因一：形状漂移**。`buildSessionItemOrder` 只认 `function_call`/`function_call_output`（`exec_command`/`shell_command`）与 `custom_tool_call`（`apply_patch`），而当前 CLI 写的是 `custom_tool_call`/`custom_tool_call_output`（`exec`）；响应里 215 个 `commandExecution` 全是 `exec-` 前缀、`session-cmd-` 零个，即**恢复什么也没注入**。逐文件扫描本机全部会话：最新最大的两个（30.89MB / 2.93MB，也就是用户真正会打开的）识别 **0** 条、新形状 222 / 7 条；更早的十个（0.08–0.88MB）识别 1–58 条。同一处漂移在别处已修过——`collectFileChangesForTurns` 注释明确写着「CLI 0.149.1+ 把 apply_patch 记录为 function_call…… 必须两种格式都收集」，`buildSessionItemOrder` 没跟上。**根因二：没有命令槽时恢复仍在重排**——`message role=assistant` 仍生成 `agentMessage` 槽，于是合并照走、只是命令槽为空，结果把 user/agent 提前、**其余全部追加轮末**，正好抹掉 app-server 已产出的流式交错（同一线程同一代码只跳过命令合并即见：原生为 `U r A C C C C r A C C C r r A M r C r A F r C C C r A`，合并后变成 `U r A r A … A C C C C C C C F F F C C C …`）。**也就是：在旧形状上它是在修 bug，在新形状上它自己就是 bug**——这解释了「命令块堆到轮末」为何反复回潮。**修复**（`src/server/bridge/session.ts` 两处）：①**形状闸门**——`buildSessionItemOrder` 回报 `sawRecoverableToolRow`（判定放在**轮次过滤之前**，描述整份日志而非当前这一页），`mergeSessionCommandsIntoTurns` 改为逐轮判断「槽里既无 `commandExecution` 也无 `fileChange` 就原样返回该轮」，且全部轮无改动时**返回入参同一引用**（`changed ? nextTurns : turns`）；②**版本判据缓存**——新增 `sessionLogRecoveryCache`（path → `{size,mtimeMs,recoverable}`，上限 64）与新入口 `mergeSessionCommandsIntoTurnsFromPath(turns, sessionPath)`：判据为假**只付一次 `stat`、不读文件**，判据为真照旧读+解析+合并（旧形状日志都小，0.88MB → 5ms），size/mtime 变则重判；`mergeSessionCommandsIntoThreadResult` 与 `threadRoutes` 的 live-state 读取都改走这个入口，两条路径共享判据。**排除过的方案**：想用「原文里有没有 `exec_command` 字样」做廉价判据——不行，该 30.89MB 日志里 `exec_command` 出现 **317 次**、`apply_patch` **56 次**，全是命令输出文本里被 grep 到/被打印出来的内容，不是行类型；判据只能读**已解析的 payload**。**验证**：新增单测 8 例（含两条缓存语义——同 size+mtime 换内容后第二次调用必须仍**不读**，构造方式是先把时间戳钉到整秒、再用等长替换 + `utimes` 复位，因为 Windows 的 mtime 带亚毫秒位；以及日志增长后必须重判）；全量 Vitest **623 通过 / 2 失败**（625 例，2 例仍是 `codexAppServerBridge.archive.test.ts` 的既有 Windows 环境性失败）、`vue-tsc --noEmit` 干净；**同环境 `git stash` A/B**（同一 dev server、同一线程、同一脚本，修复前后各两轮 × 3 次请求）热态 **1188/1011/889/1071/1280ms（中位 1071）→ 668/560/610/635/492ms（中位 610，−43%）**，**修复后最大值 668ms < 修复前最小值 889ms**；客户端可见载荷仍 **1.56MB**、`turns=10` / `threadTurnStartIndex=6` / 响应不含 `initialTurnsPage`；**10 轮条目顺序与「跳过合并」基线逐字一致**；旧形状两个会话经桥打开仍分别注入 **58/58**、**51/51** 条 `session-cmd-`；live-state 路由 HTTP 200 / 16 轮正常。**判据固化为 `scripts/probe-session-log-recovery.cjs`**（`<session.jsonl> [--require-recovery]`）：一次回答「有哪些行类型 / 恢复能不能用 / 缓存前后每次打开代价」，并带一条断言「对 ≥1MB 的日志，`stat` 必须比读+解析便宜 20 倍以上」——**CLI 升级后应跑它，若某份你预期需要时序恢复的会话报 `DOES NOT APPLY`，就是识别器又落后了**。**待办**：①**识别器落后这件事本身没有根治**——本轮只让「识别不到」变得便宜且无害，没把 `custom_tool_call exec` 认成命令；真要恢复新形状的时序恢复，必须同时解决 `custom_tool_call_output` 的输出解析，并调整**管道顺序**（当前 `sanitize` 排在合并之前，而合并注入的命令对象携带**未截断**的全量输出，一旦识别器认了新形状这些输出就会绕过瘦身直接发给浏览器）；考虑到 app-server 现在自己就产出正确交错，**更可能的正确方向是直接退役这条恢复路径**（保留 `collectFileChangesForTurns` 那条独立的 rollback 用途），但需产品决策，本轮不动。②round-84 的两条遗留仍未动：`readThreadForTurnPage` 仍是全量水合（round-84 拿到的顶层 `turnsBackwardsCursor` 正是它该用的游标）、`threadArchiveRecovery` 的辅助 resume 仍可改 `excludeTurns:true`。③版本仍停在 **0.1.124**（npm publish 由用户执行）。详见 [round-85](rounds/round-85-session-log-recovery-scope.md)。

- **round-84 打开会话去掉全量历史水合（2026-09-16，未推送、未发版）**：用户报告「消息列表加载慢」，并明确口径——**不是 DOM 慢，等待时间全部在请求 RPC 上**。**先测后改**：绕开桥直连 app-server（同 `CODEX_HOME`、同 `-c`、每形态一个全新进程避免进程内状态累积），最重一份 30.9MB / 16 轮 rollout 上量到 `thread/resume {threadId}` = **2118ms / 12.12MB**，而 `thread/read {includeTurns:false}` 只要 2–3ms —— **成本 100% 在「构造轮次」**；桥的 `trimThreadTurnsInRpcResult` 把 `thread.turns` 裁到 10 轮，但**裁剪发生在 app-server 构造完全部轮次之后**，所以裁剪/瘦身/内联图片外化全都白付（这也解释了 `ThreadReadResultCache` 为何刻意不覆盖 resume：每次都是新的全量代价）。**协议自己就写着这条路废弃**：`ThreadReadParams.includeTurns` 原文「Full-history hydration is deprecated for paginated threads; prefer a metadata-only read and page with `thread/turns/list` and `thread/items/list`.」；`ThreadResumeParams` 提供 `excludeTurns` 与 `initialTurnsPage{limit,sortDirection,itemsView}`，后者描述为「bootstrap recent turns without a second request」。**改前先验兼容性闸门**（三条全过）：①`sortDirection:"desc"` 的页是 **newest-first**，恰好等于全量水合 `.slice(-10)` 的逆序（id 序逐位相同）；②该页与 `turns/list{10,desc,itemsView:"full"}` **深度逐字相同**（item 键集 0 差异）；③游标翻页**无重叠无跳跃**、耗尽即 null（limit=5 分 4 页收齐 16 轮、去重后仍 16）。**修复**：新增 `src/server/bridge/threadResumeTurnPage.ts`（纯函数 + 注入式 RPC）——`buildThreadResumeParamsWithTurnPage` 改写请求（**不适用时返回原引用**，调用方据 identity 判断「发的是旧请求」；无 `threadId`、调用方已显式给 `excludeTurns`/`initialTurnsPage` 一律不动）；`promoteResumeTurnPage` 把页**反转成升序**挂回 `thread.turns`、写 `threadTurnStartIndex`、**删掉 `initialTurnsPage`**（不删等于同一批轮次发两遍，4.29MB→8.6MB）；`readThreadTurnCount` 用 `turns/list{itemsView:"notLoaded"}` 取总数（协议**无轮次总数接口**，游标 `rolloutOrdinal` 是 rollout 字节序位不是轮次下标；单发 `limit:10000` 覆盖现实线程、更大则游标续读上限 50 页、拿不到返回 null **不猜**）；`resumeThreadWithTurnPage` 串起全流程，**只有整页才多花那一次计数 RPC，短线程/新线程零额外请求**。接入点为 `src/server/codexAppServerBridge.ts` 的 `/codex-api/rpc` 分发处（resume 唯一入口），单点覆盖全部前端调用方。**对浏览器零改动**：响应仍是升序 `thread.turns`（≤10 轮）+ `threadTurnStartIndex`，既有管道与 `normalizeThreadMessagesV2` 一行未动。**安全网**：旧 app-server 会**忽略 `initialTurnsPage` 却仍认 `excludeTurns`** → 响应一个轮次都没有，直接渲染就是空对话；`isThreadResumeTurnPageSupported`（响应里有 `initialTurnsPage.data` 数组才算支持，**空数组也算**，那是新线程的正常形态）判定不支持时**按原参数重放一次旧请求**。**验证**：新增单测 **23 例**；全量 Vitest **615 通过 / 2 失败**（617 例，2 例为既有 `codexAppServerBridge.archive.test.ts` Windows 环境性失败）；`vue-tsc --noEmit` 干净；`scripts/probe-resume-turn-page.cjs` **7/7 PASS**（**app-server 升级后应先跑这个**）；真实桥端到端 + 同桥 A/B 见上表快照 `Dev 状态`。手测见 `tests/thread-loading-state/thread-load-capped-to-latest-10-turns.md` 新增的 Round-84 小节。**待办**：①**`readThreadForTurnPage`（上翻更早轮次）仍是全量水合**——发 `thread/read{includeTurns:true}` 再 slice，首调约 1s、之后靠 20s 缓存；本轮拿到的顶层 `turnsBackwardsCursor`（已在响应里原样透出）正是它该用的 `cursor`，改成真分页是有界的下一步。②**桥侧管道成本现在浮上来了**：app-server 侧新路径只要 198ms，而经桥端到端 734–1242ms，差值来自内联图片外化 + skill/命令合并（要读 session 日志）等**改动前后同样发生**的既有步骤，不是本轮回归但已成主要成本，值得单独剖面。③`threadArchiveRecovery` 里 `turn/start` 失败后补的那次 `thread/resume` 调用方丢弃返回值，可顺带改成 `excludeTurns:true`（罕见救济路径，本轮未动）。④**版本仍停在 `0.1.124`**（npm publish 由用户执行），本轮改动随下一次发布一起走。详见 [round-84](rounds/round-84-open-thread-bounded-hydration.md)。

- **round-83 客户端自动压缩在长 turn 下不触发（2026-09-15，未发版）**：用户报告「自动压缩还有问题，正在 Thinking 的时候 size 超出了」，口径确认为**长时间 turn（Thinking + 工具调用）进行中上下文涨过阈值甚至超过窗口，而客户端全程没有任何压缩动作**。**先测后改**（真实 rollout 数据，最重一份 2.89MB / 29 turn）：①服务端确实会自动压缩，且发生在 **turn 内**（压缩记录落在 turn 3 与 turn 14 的内部，不是 turn 边界）；②服务端触发点 ≈ **90% 已用**（两次压缩所在 turn 的峰值 116,890 / 113,473 tokens，窗口 121,600 → 96.1% / 93.3%）；③单个长 turn 吃 **4–10% 窗口**（+11.7k / +4.7k）；④本机 `config.toml` 未配 `model_auto_compact_token_limit`，走内置默认。**根因**：客户端阈值只被「发送前预检」消费，而该预检在 turn 进行中按设计跳过（round-28 问题 3），`setThreadInProgress(false)` 又只补发暂存、**不做压缩** → 用户不再发送时压缩永不发生。**修复**（`src/composables/useDesktopState.ts` 2 处）：新增 `shouldAutoCompactOnTurnEnd(threadId)`（与发送前预检共用阈值与防重入条件），`setThreadInProgress(false)` 改为「命中阈值 → `compactThreadById`，否则 → `flushStashedForThread`」（压缩收口内部本就会补发暂存，两条分支都不漏补发）。**刻意不挂在 `thread/tokenUsage/updated` 上**：Thinking 期间用量事件高频，若压缩后用量仍 ≤ 阈值会形成「压缩 → 用量事件 → 再压缩」循环；挂在 turn 结束这一状态迁移上每轮最多一次。**验证**：新增单测 2 例（turn 内不压缩 / turn 结束即压缩、用量高于阈值时不压缩）；**判别力 A/B**——`git stash push -- src/composables/useDesktopState.ts` 移出源改动后新用例失败（`expected "vi.fn()" to be called with arguments: [ 'thread-auto-compact' ]`），恢复后通过；全量 Vitest **591 通过 / 2 失败**（593 例，2 例为既有 `codexAppServerBridge.archive.test.ts` Windows 环境性失败）、`vue-tsc --noEmit` 干净、`vite build` 通过。手测见 `tests/chat-composer-rendering/client-side-auto-compact-pre-send-stash-resend.md`（第 13–15 步）。**待办（用户 2026-09-15 决定）**：①默认阈值维持 **10%** 不改——已记录「客户端 10% 剩余 = 服务端 90% 已用、两者同点，客户端无法抢先」这一事实，若日后要客户端稳定接管可上调设置项或把默认值改到 20% 左右；②**版本暂不 bump**，工作区停在 `0.1.124`（该版本本身尚未 publish，用户已决定暂缓），本轮改动随下一次发布一起走。详见 [round-83](rounds/round-83-auto-compact-turn-boundary.md)。

- **round-82 systemd 停止超时修复（2026-09-15，v0.1.124 已发布）**：用户报告 `systemctl restart codexapp.service` 每次都留一条 systemd 失败行（`Failed with result 'exit-code'` 或 `Failed with result 'timeout'`）。用户已自行定位「两个 5 秒定时器赛跑」（systemd `DefaultTimeoutStopSec=5s` → `TimeoutStopUSec=5s` vs 应用 `shutdown()` 里的 5000ms 兜底），并已在 service 层加 drop-in `TimeoutStopSec=30` 缓解、指出 app 层源码才是根治（`ExecStart` 指向包内 `dist-cli/index.js`，只有改产品源码才能随升级生效）。本轮用真实 Node 22.22.2 + 真实 `ws` 探测收窄根因：**keep-alive 连接不持有 `server.close()`（回调 1ms），websocket 才是持有者，且 `closeIdleConnections()`/`closeAllConnections()` 对升级后的 socket 完全无效**（Node 在 `upgrade` 后把 socket 移出 tracked connections，两方法都遍历不到，而 `server._connections` 仍计 1）——**单纯补这两个 API 修不好**；只有显式 `terminate()` 客户端能释放（2ms）；SSE 等活跃 HTTP 长响应才需要 `closeAllConnections()`（1s 宽限后）。修复：`src/server/httpServer.ts` 新增导出 `shutdownWebSocketServer(wss)`（terminate 全部 clients + `wss.close()`），`ServerInstance.attachWebSocket` 签名改为返回清理函数；`src/cli/index.ts` 的 `shutdown()` 先调清理 → `server.close()` → `closeIdleConnections()` → 1s 后 `closeAllConnections()`，并把 5 秒兜底定时器**提到最前注册**（避免清理抛错时跳过兜底、挂到 systemd 停超时）、`closeIdleConnections` 加 `typeof === 'function'` 防护（`engines` 声明 `>=18` 但该 API 是 Node 18.2 引入）。验证：新增 `src/server/httpServer.websocketShutdown.test.ts` 2/2（含「有 ws 客户端时 `close()` 不回调」基线断言）、全量 Vitest **589 通过 / 2 失败**（基线 587/2，+2 为本轮新增；2 例为既有 `codexAppServerBridge.archive.test.ts` Windows 环境性失败）、`vue-tsc --noEmit` 干净（`TSC_EXIT=0`）、tsup CLI 构建通过、真实 dist-cli 端到端（启动日志 / `GET /` 200 / `/codex-api/ws` 握手 `{"method":"ready"}`）通过、`dist-cli/index.js` 含 `client.terminate()` 与两个 `close*Connections`。手测见 `tests/cli-network-platform/systemd-stop-finishes-inside-timeout.md`。**待办（2026-09-15 更新：①已闭环，②③情况见下，④新增）**：①**OS 信号投递与 systemd 日志均已闭环**——先用一次性探针（`tmp/shutdown-signal-probe.cjs`，绕过信号投递、经 Node inspector 在运行中的真实进程内 `process.emit('SIGTERM')` 调用 `SIGINT`/`SIGTERM` 所绑定的同一个 `shutdown()`）在打包产物上做了 A/B：修复前 **exit 1 @ 5015ms**、修复后 **exit 0 @ 11ms**（2 个真实 ws 客户端）/ **10ms**（0 个），即连接清理、`server.close()` 回调、退出码与时延都已在 Windows 侧端到端闭环；该环节随后在 WSL2 真实 Linux 上投递**真实内核 SIGTERM** 复测（修复前 **exit 1 @ 5009ms** → 修复后 **exit 0 @ 7ms**，`signal=null` 证明是 handler 主动 `exit(0)` 而非内核默认杀死），并临时启用 systemd 249（`/etc/wsl.conf` 的 `[boot] systemd=true`，跑完已还原）用探针 unit + `tmp/hold-ws.cjs` 保持 2 个 ws 连接跨停止：pre-fix `Result=exit-code/ExecMainStatus=1` + journal `Failed with result 'exit-code'.` 与用户报告逐字一致，post-fix `Result=success/0`、`systemctl stop` 14ms、`restart` 40ms、无失败行；②npm publish **用户决定暂缓**（截至 2026-09-15 复查，registry 上 `dist-tags.latest` 仍为 **0.1.123**，**0.1.124 未发布**）；③用户 service 层的 `TimeoutStopSec=30` drop-in 未触碰，修复后不再是常规路径但建议保留作缓冲；④round-81 的线上救济仍挂起——脚本**不需要线程 id**（自动枚举 `preview=''` 的行），缺的是线上机器访问权（本机 `CODEX_HOME` 复查为 31 行线程 / 0 个空 preview，确认受害者只在线上）。**另记两点**：`dist-cli/` 未入库（构建产物，`prepublishOnly` 会重建，不影响发布），但本轮开始时工作区那份仍是 **v0.1.123 旧产物、不含本次修复**，凡复核产物须先 `tsup` 重建；`--port 0` 时 `listenWithFallback` 回传尝试值，banner 的 `Bind:` 与 `CODEXUI_SERVER_PORT` 会显示 `0` 而实际监听随机端口（独立小缺陷，本轮未改）。

- **round-81 已消失线程的救济路径（2026-09-14，随 v0.1.123 发布）**：用户指出 round-80 的受害者出现在**线上环境**（本机那份 `CODEX_HOME` 里确实没有受害者，故结论只能靠受控复现）。为线上线程找救济时，实测**两次推翻** round-80 的收尾结论：①直写 `state_*.sqlite` 的 `preview` 列**无效**——服务运行中改列 `thread/list` 不变，停机改完冷启动该行**仍不可见**（同库内会话中新建的正常线程可见），说明 **rollout 才是元数据事实源、sqlite 只是写穿缓存**；②协议里确有一条侧门：`thread/goal/set` 会往 **rollout** 追加 `thread_goal_updated` 事件，而元数据派生在首消息正文为空时回退到 goal objective；③关键前提：必须**先 `thread/resume`** 把线程加载起来，否则 RPC 返回 OK、缓存列也更新，但 rollout 里什么都不写、列表不变、重启后又派生回空。救济序列 = `thread/resume` → `thread/goal/set`（objective 填推导文案、status=complete）→ `thread/goal/clear`（实测清掉 goal 后 preview 仍保留、`goal/get` 为 null、rollout 不留 cleared 事件）。新增 `scripts/rescue-empty-preview.mjs`（默认 dry-run；读库只读；走运行中服务的 `/codex-api/rpc`，回环免鉴权；判据用 rollout 而非 `has_user_event`——后者在失败 turn 上不置位）。验证：隔离 `CODEX_HOME` 造两条真实受害者（附件前言+空正文 / 空文本块），走**真实 dist-cli 服务**：基线 `thread/list` 0 条 → 救济后 **2 条可见**（`clip.mp4` / `[Attachment]`）→ 服务重启**仍在**；`vue-tsc` 无类型变化、单测沿用 round-80 基线 587 通过 / 2 既有环境性失败。另修正 round-80 文档与 `src/utils/turnPromptText.ts` 注释里已被证伪的「只能预防、事后修不回来」结论。**待办**：未对线上任何真实库执行救济（尚未拿到受害者 id / rollout 路径）。版本已随 v0.1.123 发布。
- **round-80 附件/图片首消息正文为空导致线程消失（2026-09-14，随 v0.1.123 发布）**：用户反馈一条线程「消失」。排查（二进制 + 隔离实例实测，详见 round-80）：①codex 0.153.4 的列表查询 WHERE 片段含 `AND threads.preview <> ''`，部分索引同样带该谓词，空 preview 的行级不可见；②用隔离 `CODEX_HOME` 起 app-server 回放三种首消息：附件前言+空正文 与 空文本块 两种形态落库 `preview/title/first_user_message` 全为空串（3 行仅 1 行可见），纯文本对照正常 —— 说明 app-server 是剥掉前言取正文；③`thread/metadata/update` 只能改 `gitInfo`/`projectId`，`thread/name/set` 只写 `name`，列表谓词只看 `preview`，**没有直接写 `preview` 的 RPC**（round-81 更正：「事后完全修不回来」不成立，`resume + goal/set` 可救济）；④本机存量 30 行 0 空 preview，本轮为预防性修复。改动：新增 `src/utils/turnPromptText.ts` 的 `resolveTurnPromptText`（正文非空则原样返回，否则回退 首个附件 label → `[Image]` → `[Attachment]`），接入 `startThreadTurn` 与 `buildQueuedTurnParams` 两处组装点（`buildTextWithAttachments` 职责不变）。验证：修复后同口径回放 app-server 得 `preview='clip.mp4'`、2/2 行可见（PASS）；新增 2 个测试文件共 7 例全过；全量 Vitest **587 通过 / 2 失败**（既有 Windows 环境性失败）、`vue-tsc` 干净、`vite build` 与 `tsup` 通过。手测见 `tests/thread-loading-state/attachment-only-first-message-keeps-thread-listable.md`。**待办**：无（已随 v0.1.123 发布并推送）。

- **round-79 回退后消息列表倒序修复（2026-09-14，随 v0.1.123 发布）**：用户反馈「回退的时候会导致消息列表渲染顺序错误，比如最老的一条消息显示在最新或最下面」。排查：全仓只有 `revertThread` 消费 `thread/turns/list`；实测 dev server RPC 确认 `sortDirection: 'desc'` 返回页 **newest-first**（首个 turn `019feaa1` 新于 `019fea87`，UUIDv7 时间有序；schema 亦写明默认 descending），而 `normalizeThreadMessagesV2` 保序且按输入顺序递增 `turnIndex`——round-74 的 `revertThread` 未反转 desc 页直接消费，导致 paginated 回退后保留历史**整段倒序**（最老消息沉底）且 `turnIndex` 全部反向；且回退后 silent 重载走 `mergeMessages(preserveMissing: true)` 保留既有排布，错误顺序不自愈。修复：`revertThread` 在 normalize 前 `[...page.data].reverse()`（[threads.ts:600](file:///d:/code/codex-mobile/src/api/gateway/threads.ts#L600)），hydrate 回到时间序。新增 `threads.revertOrder.test.ts` 2 例；定向 Vitest 97/97、全量 **580 通过 / 2 失败**（既有 Windows 环境性失败）、`vue-tsc` 干净、`pnpm run build` 通过。代码提交 `f2cd90a`（含手测文档 round-79 节）。性能审计：回退一次性动作中 ≤200 元素 O(n) reverse，无新增请求/缓存，代码路径分析即可。提交 `f2cd90a`、`556d302` 已推送 `origin/main`。**待办**：无（已随 v0.1.123 发布）。

- **v0.1.122 发布（2026-09-11，round-78，已全部闭环）**：版本 `0.1.122`（性能与缓存提交 `66b6996`，文档/版本提交 `a2d43c0`，哈希记录 `d87859d`，推送状态提交 `3393175`；均已推送 `origin/main`）。git tag `v0.1.122`（annotated，指向 `3393175`）与 GitHub Release 已由维护者创建（非草稿/非预发布，已标记 Latest）：https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.122 ；`codex-mobile-re@0.1.122` 已由用户 publish 至 npm 官方源并成为 `latest`（`dist-tags.latest` → `0.1.122`，发布时刻 `2026-09-11T13:27:47.938Z`，shasum `3a331aab8ba9ea361a4c071498a964193fbbcf9e`），发布链路全部闭环。用户要求「配置好 `@colbymchenry/codegraph` MCP 后再排查一次」。①**MCP 接入**：codegraph v1.5.0 已由 pnpm 全局安装、项目 `.codegraph/` 索引已存在；因 pnpm 全局目录为版本哈希目录（`codegraph upgrade` 后会失效），新增稳定启动器 `~/.workbuddy/bin/codegraph-mcp.cjs`（运行时搜索全局根下的 `npm-shim.js` 并委派），`~/.workbuddy/mcp.json` 合并追加 `codegraph`（`serve --mcp`）；握手已验证（`serverInfo={name:codegraph,version:1.5.0}`、`tools/list=[codegraph_explore]`）。**待办：MCP 改动不会自动生效，需在连接器管理页右上角自定义连接器入口对 `codegraph` 点「信任」后才加载**（本会话内未刷新，故复查用等价 CLI）。②**复查与修复**：稳态切换已快（点击 #1/#2 82–264ms），但首次点击固定 ~1.1s 且与线程内容无关；时间线显示 `thread/resume` 仅 32ms 却等到 +1221ms 才发出（紧随 `provider-models` 结束）。A/B 归因：单独短路 `/codex-api/provider-models*` → 首次点击 1102→**126ms**；短路 `free-mode/status` 或 `/codex-api/git/**` 均无变化（**更正 round-77 把 `git/branches` 500 归因于「git 不可用」的说法**：git 可用，只是本机单条 git 命令即需 316–488ms、该路由串行跑 4–6 条）。根因是 `selectThread` 里 `await refreshModelPreferences({ includeProviderModels: true })` 把外部 provider 目录放在打开线程关键路径上，而服务端 zen/custom 目录请求每次调用都打外部接口（无缓存）、openrouter 的 `getFreeModels()` 只在成功时写缓存且其 `fetch` 无超时。改动 3 文件：`src/server/bridge/models.ts`（模块级目录缓存：TTL 10min／空结果 1min／in-flight 去重／过期先发后用）、`src/server/freeMode.ts`（1.5s 超时 + 失败也记账 + 兜底只压 60s）、`src/server/codexAppServerBridge.ts`（新增 `warmProviderModelCatalog()` 并接入既有启动预热块）。实测 `provider-models` **1276→3ms**、首次点击 **1102→~112ms（−90%）**。新增 2 个测试文件（6 例全过）；全量 Vitest **578 通过 / 2 失败**（既有 Windows 环境性失败）、`vue-tsc` 干净。已随 **v0.1.122** 发布（与 round-77 同批）。详见 [round-78](rounds/round-78-provider-catalog-cache-open-thread-path.md)。

- **round-77 线程打开时延（2026-09-11，随 v0.1.122 发布）**：用户反馈 round-76 后「左侧线程加载快了一点但还是有点慢；消息列表感觉没有变化」。测量确认 round-76 减的是响应字节，而打开线程的耗时由**并发请求争抢**主导（尤其轮耗时镜像的 PUT 风暴）。两处改动已落地并验证：①`savePersistedTurnDurationMap` 改为**只发增量**（`mirroredTurnDurations` 快照 + `seedMirroredTurnDurations`；启动合并服务端存档后不回写）——本机启动 PUT 由 27 → 0；②`provider-models` 加 in-flight 去重（首屏 ×4 → ×2）。实测 `thread/resume` 224→81ms（−64%）、点击线程 #1/#2/#3 −47%/−65%/−49%、`thread-turn-durations` 请求 28→1、请求总数 85→53、boot→侧栏 403–476ms（持平）。新增 `useDesktopStatePersistence.turn-durations.test.ts` 3/3；全量 Vitest 572 通过/2 失败（既有 Windows 环境性失败）、`vue-tsc` 干净。已随 **v0.1.122** 发布（与 round-78 同批）。详见 [round-77](rounds/round-77-turn-duration-mirror-and-provider-models-dedupe.md)。
- **v0.1.121 发布（2026-09-11，round-76，已全部闭环）**：版本 `0.1.121`（性能优化提交 `0d18ca2`，文档/版本提交 `3e6405c`，哈希记录 `449cbc1`，推送状态 `b8642f2`）。收录线程/消息列表加载性能优化的四项桥层改动，基线以真实最重线程 `01a0465a-f570-78a0-ae6b-badc1c40b4e8` 实测（该线程 rollout 两段 3.07MB + 32.4MB）：改动前 `thread/read` 13.55MB（19 轮未裁剪）、前端打开 `thread/resume` 3041.8ms / 4,496,900B、首屏 `/codex-api` 合计 4,448.4KB，其中 92% 是 UI 从不整段渲染的大文本（`commandExecution` 74% 单块最大 803KB、`mcpToolCall.result` 18% 占 1.52MB 且前端无消费点）。①删 `mcpToolCall.result`；②`aggregatedOutput` 超 16KB 中段截断（保头 60% + 尾 40%，按字节切分后剥接缝 U+FFFD）+ sha1 落盘（>8MB 只截断不落盘）+ 新增 `GET /codex-api/command-output?ref=`（ref 走 `/^[0-9a-f]{40}$/` 白名单，实测穿越尝试 400、不存在 404、合法 13.6ms 取回 36,841B），前端加截断提示行与「查看完整输出」入口；③`ThreadReadResultCache`（TTL 20s、≤6 条、`turn/`+`thread/start|fork|rollback|revert|resume|archive|unarchive|delete|compact|name/` 命中即失效，**不缓存 `thread/resume`**），重复 `thread/read` 1745ms → 72ms；④`AppServerProcess.warmUp()` 在桥创建时预热——实测只能部分重叠冷启动（前端 1–3s 到达 vs init ~2.1s），价值在后续加载而非首屏，已如实写进代码注释；副作用是 spawn 由懒触发变为桥创建即触发（`createServer` 打包形态同样适用）。实测最重线程响应体 **4,391.5KB → 1,599.1KB（−63.6%）**、首屏总字节 **4,448.4KB → 2,010.3KB（−54.8%）**、命令输出该类型 **−70.8%**（215 块中 31 块超限）、热态端点全部 ≤70ms（无回归）。新增 `payloadSlimming.ts`（+测试）、`threadReadCache.ts`（+测试）、`commandOutputRoute.test.ts`；`v2.test.ts` +3 例。全量 Vitest **569 通过 / 2 失败**（`codexAppServerBridge.archive.test.ts` Windows 权限差异，既有环境性失败）、`vue-tsc --noEmit` 干净、`vite build`（335 模块）通过。顺带修复既有 flake：`useDesktopState.test.ts` 补 `persistThreadTurnDuration` mock。手测见 `tests/thread-loading-state/thread-payload-slimming-and-output-spill.md`。git tag `v0.1.121`（annotated，指向 `b8642f2`）与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.121 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.121` 已由用户 publish 至 npm 官方源并成为 `latest`（`dist-tags.latest` → `0.1.121`，发布时刻 `2026-09-11T08:05:17.895Z`），发布链路全部闭环。详见 [round-76](rounds/round-76-thread-loading-perf.md)。
- **v0.1.120 发布（2026-09-11，round-75，已全部闭环）**：版本 `0.1.120`（修复提交 `8852718`，文档/版本提交 `bfcf11b`）。收录一处侧边栏重复线程行修复——paginated 线程经 `thread/revert` 后不重写原 rollout 文件，而是新开一段并以 `history_base` 指向前缀（预期行为）；app-server 从 `sessions/` 目录扫描构建 `thread/list`，把同一 session 的多段 rollout 当成多条线程返回（`state_*.sqlite` 仍是「一 id 一行」），导致侧边栏同一线程渲染多行并触发 Vue duplicate-key 告警。修复：bridge 层 `rpcPipeline.ts` 新增 `dedupeThreadListByIdKeepNewest`，作为 `thread/list` 的终态步骤按 `id` 归并、保留 `updatedAt` 最大（最新）段、保持首次出现顺序、无 id 行透传、无重复时返回原引用；前端新增纯函数 `src/utils/threadGroups.ts` 的 `dedupeThreadGroupsById`，`SidebarThreadTree.vue` 加 `dedupedGroups` 计算属性把四处渲染源（`filteredGroups`/`globalThreads`/`threadById`）全部接上兜底，不改 `loadThreads` 的「服务端权威、直接替换」语义。定向 Vitest `threadGroups` 4/4 + `rpcPipeline` 5/5、全量 541 通过/2 失败（`codexAppServerBridge.archive.test.ts` Windows 权限差异，既有环境性失败）、`vue-tsc --noEmit` 干净、`vite build` 与 `tsup` CLI 构建通过。手测见 `tests/thread-loading-state/sidebar-deduplicates-paginated-thread-segments.md`。git tag `v0.1.120` 与 GitHub Release 已创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.120 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.120` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.120`），发布链路全部闭环。详见 [round-75](rounds/round-75-dedupe-paginated-thread-segments.md)。
- **v0.1.118 发布（2026-09-10，round-73）**：版本 `0.1.118`（需求实现提交 `8aced6f`，含 bug A/B 修复、回退降级与协议快照的最终提交稍后追加）。收录三方面改动：①切换模型后上下文窗口未更新——`invalidateThreadContextWindow` 把 `modelContextWindow` 置 null（保留 token 计数），指示器进「等待新模型首个 tokenUsage 事件」占位态，避免继续显示旧模型窗口；②模型切换分割栏——`injectModelSwitchDivision` 往消息列表按切换锚点注入本地持久化「旧→新」分割栏（`codex-web-local.thread-model-switch-markers.v1`，≤20/线程，不写服务器）。`ThreadConversation` 重构 `renderTurns` 为多分割栏锚定（leadingDividers/dividers 数组），修复三大问题：分割栏文本不居中、重复切换堆积、新消息跑到上次用户消息下方；`filteredMessages` 剔除 modelSwitch，重复切换原地更新单条；③回退降级——`threads.ts` 新增 `revertThread(threadId, beforeTurnId)`，`useDesktopState` `rollbackThreadWithRevertFallback` 在 paginated 历史拒绝 `thread/rollback`（`not support thread/rollback`）时降级 `thread/revert`，并改为**先退对话后退文件**、`console.warn` 显式上报文件回退错误；④协议快照同步——`documentation/app-server-schemas/{json,typescript}` 用本机 codex `0.153.4`（`--experimental`）重新生成（json 416 / ts 827 文件），`APP_SERVER_DOCUMENTATION.md` 更新。`modelSwitchMessages.test.ts` 2、`useDesktopState.test.ts` 93、`transcriptGrouping.test.ts` 33 通过，`vue-tsc --noEmit` 干净，浏览器实测分割栏四例 + 回退 paginated/legacy 全过。git tag `v0.1.118` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.118 ，非草稿/非预发布）；`codex-mobile-re@0.1.118` 已由用户 publish 至 npm 官方源并成为 `latest`（发布时刻 `2026-09-10T16:18:43.363Z`，shasum `3fddde93e5c5ba99f4fe30a0d66f0d71dfc11fdf`，2026-09-15 复查回填）。详见 [round-73](rounds/round-73-model-switch-context-and-divider.md)。
- **v0.1.117 发布（2026-09-10，round-72，已全部闭环）**：版本 `0.1.117`（修复 `e5e1efd`，版本/文档提交 `a08fd3f`）。收录一处线程模型切换失效修复——`startTurnForThread` 对「本会话尚未 resume 过的线程」发首个 turn 前 `resumeThread` 会**无条件**把服务端线程持久化的 model 写回覆盖 UI 选择：用户在 UI 把线程从已下线/删除的旧模型（如 `deepseek-v4.1-flash-expires-on-0910`）切到可用新模型后，首个请求仍带旧模型 ID → LiteLLM 400。修复：新增 `hasThreadModelSelection(threadId)`（[useDesktopModelPreferences.ts](file:///d:/code/codex-mobile/src/composables/useDesktopModelPreferences.ts)），在 [useDesktopState.ts `startTurnForThread`](file:///d:/code/codex-mobile/src/composables/useDesktopState.ts#L2829-L2845) resume 覆盖前加门控——线程已有显式 UI 选择时不覆盖；无显式选择仍用服务器 model 初始化（无回归）。`useDesktopModelPreferences.test.ts` 4/4、`useDesktopState.test.ts` 93 通过、`vue-tsc --noEmit` 通过。手测见 `tests/providers-models/thread-model-switch-persists-on-resume.md`。git tag `v0.1.117` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.117 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.117` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.117`），发布链路全部闭环。详见 [round-72](rounds/round-72-fix-thread-model-switch-on-resume.md)。
- **v0.1.116 发布（2026-09-08，round-71，已全部闭环）**：版本 `0.1.116`（修复 `79622df`，版本/文档提交 `a87f5d8`）。收录一处空过程行修复——推演消息（`agentMessage` 等）出现在本轮过程区，当其 `text` 为空、又无图片/文件附件/技能时，落入 [ThreadConversation.vue](file:///d:/code/codex-mobile/src/components/content/ThreadConversation.vue) 的通用正文分支：`message-card`（`v-if="message.text.length > 0"`）被跳过、附件/技能也为空 → 渲染出完全空的 `<li class="conversation-item conversation-item-process" data-message-type="agentMessage">`。新增纯函数 `src/utils/messageContent.ts`（`hasMessageBodyContent` + `shouldOmitEmptyGenericMessage`），普通 `<li>` 分支（`v-else-if`）追加 `&& !shouldOmitEmptyGenericMessage(message)`——仅在命中通用正文分支且无任何可渲染内容时省略，不触碰 command/toolCall/fileChange/compaction/plan 专用分支。空 `processFold`（全隐藏折叠）自 v0.1.115 已修复；若在 v0.1.115+ 仍见空折叠，为旧构建方会如此。`vue-tsc --noEmit` 通过、`messageContent.test.ts` 5/5、`conversationFolds.test.ts` 15/15 通过。git tag `v0.1.116` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.116 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.116` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.116`），发布链路全部闭环。详见 [round-71](rounds/round-71-fix-empty-process-row.md)。
- **v0.1.115 发布（2026-09-08，round-70，已全部闭环）**：版本 `0.1.115`（修复提交 `f2233bb`，版本/文档提交随行）。收录一处空 `processFold` 块修复——折叠分组 `buildProcessFolds` 按「同轮次连续命令/工具」成组，而命令分组 `groupedCommandsByLatestId` 按「连续命令·不区分轮次」分组；当相邻两轮末尾/开头各带命令时，下一轮命令成为跨轮命令块的最新命令，本轮折叠全部命令被 `hiddenGroupedCommandIds` 隐藏 → 渲染出只有折叠头、无任何成员的空 `processFold` 块。新增纯函数 `isProcessFoldEmpty` + `emptyFoldStartIds`，模板跳过空折叠 `<li>` 渲染（成员内容已在跨轮命令块/文件变更摘要展示，不丢数据）。`vue-tsc --noEmit` 通过、`useDesktopState`/`conversationFolds` 定向测试通过（`conversationFolds.test.ts` 15/15，新增 `isProcessFoldEmpty` 两条用例）。git tag `v0.1.115` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.115 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.115` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.115`），发布链路全部闭环。详见 [round-70](rounds/round-70-fix-empty-processfold-block.md)。
- **v0.1.114 发布（2026-09-08，round-69，已全部闭环）**：版本 `0.1.114`（提交 `63417f3`，版本/文档提交 `8d563ca`）。收录一处回退健壮性修复——`rollbackSelectedThread` 目标轮 turnIndex 无法解析（如通知增量通道刚写入、缺 `turnIndex` 且映射表也未登记）时不再静默 `return`（曾导致「点了回退没反应、最后一条消息还在」，且不报错），改为钳制到最新一轮再回退并 `console.warn` 留痕；对真实 codex `0.149.1` app-server `thread/rollback numTurns` 语义做端到端验证（`thread/fork` 副本 28 轮 rollback 1 → 27 轮，正确删除末尾 userMessage，schema `ThreadRollbackParams.json` 亦写明 `numTurns >= 1`）。`vue-tsc --noEmit` 通过、`useDesktopState.test.ts` 93/93 通过（含新增「turnIndex 未解析时钳制到最新轮调用 `rollbackThread`」用例）。git tag `v0.1.114` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.114 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.114` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.114`），发布链路全部闭环。详见 [round-69](rounds/round-69-v0.1.114-rollback-no-silent-nop.md)。
- **v0.1.113 发布（2026-09-04，round-68，已全部闭环）**：版本 `0.1.113`（提交 `e1be9d9`，版本/文档提交随后）。收录一处回退语义修复——`rollbackSelectedThread` 的 `numTurns` 从 `Math.max(1, maxTurnIndex - turnIndex)` 改为 `maxTurnIndex - turnIndex + 1`：回退目标轮移除该轮（含其用户消息）及其后的所有轮次，而非只删后续、保留目标轮（此前「确认回退后消息列表没有更新，回退那条消息还在列表」）；目标轮即最后一轮时 `+1` 后仍为 1，删除该轮而非静默无操作。`vue-tsc` 通过、`useDesktopState.test.ts` 92/92 通过、浏览器实测回退首/中/末消息均生效。git tag `v0.1.113` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.113 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.113` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.113`），发布链路全部闭环。详见 [round-68](rounds/round-68-v0.1.113-rollback-target-turn.md)。
- **v0.1.112 发布（2026-09-03，round-67，已全部闭环）**：版本 `0.1.112`（提交 `b53ee3f`，发布提交 `5947bda` 之后新增 `9b15b8c` + `b53ee3f`）。收录两处改动：①线程切换卡顿优化——`models.ts` 为 `/codex-api/provider-models` 增加 30s TTL 缓存（`fetchProviderModelIds`），切换线程不再重复请求（此前每次 340~3600ms）；`App.vue` 用 `lastStableFilteredMessages` 保留上一次稳定消息列表，加载期间显示旧内容避免闪烁；`useDesktopMessageHistoryLoading.ts` 引用计数管理并发加载的 `isLoadingMessages`；②回退最后一条消息修复——`rollbackSelectedThread` 目标轮即最后一轮时 `numTurns` 为 0 此前静默无操作，改为 `Math.max(1, maxTurnIndex - turnIndex)` 移除该轮本身。`vue-tsc` 通过、`useDesktopState.test.ts` 91/91 通过、浏览器实测回退最后一条消息生效。git tag `v0.1.112` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.112 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.112` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.112`），发布链路全部闭环。详见 [round-67](rounds/round-67-v0.1.112-thread-switch-perf-rollback-last.md)。
- **v0.1.111 发布（2026-09-02，round-66，已全部闭环）**：版本 `0.1.111`（提交 `aca350e`，发布提交 `5947bda`）。收录三处改动：①回退不再静默失效且保留目标轮次——`rollbackSelectedThread` 的 `turnIndex` 缺失时从 `turnIndexByTurnIdByThreadId` 兜底，`numTurns` 从 `maxTurnIndex - turnIndex + 1` 修正为 `maxTurnIndex - turnIndex`（此前回退 1 轮会连目标轮一并删除）；②服务端 `collectFileChangesForTurns` 解析 CLI 0.149.1+ 的 `function_call` 格式 apply_patch（patch 在 `arguments.command`），与旧版 `custom_tool_call.input` 双格式兼容，回退文件变更不再报「No turns to revert」；③现有线程（空闲/进行中）发送消息立即显示乐观用户行，不再先 `Thinking` 再等用户消息。`vue-tsc` 通过、`pnpm run build` 通过（web + CLI）、`useDesktopState.test.ts` 90/90 通过，浏览器实测三种发送路径乐观 UI 均生效。git tag `v0.1.111` 与 GitHub Release 已由维护者创建（https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.111 ，非草稿/非预发布，已标记 Latest）；`codex-mobile-re@0.1.111` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.111`），发布链路全部闭环。详见 [round-66](rounds/round-66-v0.1.111-rollback-optimistic-ui.md)。
- **v0.1.109 发布（2026-08-29，round-64，已全部闭环）**：版本 `0.1.109`（提交 `71e7cd8`）。收录今天三处改动：①回收站弹窗固定高度（`recycle-bin-content` 容器 `h-64 overflow-y-auto`）；②思考强度无显式选择时默认 `medium`（`useDesktopModelPreferences.ts`）；③`ComposerPopover` 面板改 viewport `fixed` 定位，修复 H5 下「+」附加菜单被 `.thread-composer-controls` 的 `overflow-x-auto` 滚容器裁剪不可见。`vue-tsc` 通过、`pnpm run build` 通过（web + CLI）、移动端 Browser Use 实测三项全部 PASS（菜单可弹出、回收站固定高度、推理强度 Medium）。git tag `v0.1.109` 与 GitHub Release 由维护者创建；`codex-mobile-re@0.1.109` 已由用户 publish 至 npm 官方源并成为 `latest`（`npm view codex-mobile-re dist-tags.latest` → `0.1.109`），发布链路全部闭环。详见 [round-64](rounds/round-64-v0.1.109-recycle-bin-thinking-h5.md)。
- **v0.1.108 发布（2026-08-29，已全部完成）**：版本 `0.1.108`（`4e9fe75` bump）。收录每轮耗时显示（`d3b3eb5`）、props TDZ 白屏修复（`1edd0cd`）、8 个 ThreadConversation hook 抽取与 App.vue `useSidebarUi`/`useRightPanel` 抽取、`useReplyCopyFork` 复位计时器卸载清理补回（`dff3944`）。hook 抽取逐行对照无行为漂移；`vue-tsc` 通过、Vitest 507/509（2 个失败为 `codexAppServerBridge.archive.test.ts` 的 Windows 文件 mode/符号链接环境性旧问题）。main 已同步至 `e943856`（`dc839b6`/`004299d`/`e943856` 为发布与交接文档提交）；tag `v0.1.108`、GitHub Release 与 `codex-mobile-re@0.1.108`（npm `latest`）全部闭环，见 https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.108。详见 [round-63](rounds/round-63-v0.1.108-hooks-and-release.md)。
- **round-61 v0.1.107 线程状态修复（2026-08-27，已发布）**：版本已升至 **`0.1.107`**。修复 `thread/list` 在异步后处理期间 tracker 扫描完成却仍使用旧子代理排除快照的问题；修复新 live overlay 出现但 `liveTurnId` 尚未知时，上一轮最终总结被误判为活跃轮过程项的问题。两项均已补最小 Vitest 回归用例，并更新线程加载/状态手测说明；生产构建 `pnpm run build` 已通过，合并定向 Vitest（`rpcPipeline.test.ts`、`externalSessionTracker.test.ts`、`transcriptGrouping.test.ts`、`transcriptGrouping.repro.test.ts`）**58/58** 通过；浏览器手测未做。修复提交 `fdbedb8` 已推送，tag 与 GitHub Release `v0.1.107` 已创建；`codex-mobile-re@0.1.107` 已发布至 npm 官方源并成为 `latest`。详见 [round-61](rounds/round-61-v0.1.107-thread-state-fixes.md)。
- **round-60 桌面前后台恢复同步待手测（2026-08-27）**：恢复同步已从移动端扩展至桌面和移动端，并以 400 ms 后台阈值与单次触发标记合并 `visibilitychange`、持久化 `pageshow`、`focus` 信号。新增 `foregroundResume` 单测 **91/91 通过**，`vue-tsc --noEmit` 与生产构建均通过。代码提交 `01ab0b4` 与交接文档提交 `cbc1846` 均已推送；桌面/移动端浏览器手动验证仍待执行。详见 [round-60](rounds/round-60-desktop-foreground-resume-sync.md)。
- **v0.1.106 发布（2026-08-26 round-59）**：收录 round-57（过程收尾记录不再遮蔽最终总结）和 round-58（`turn/completed` 先到、最终消息暂留 `.live` 时总结不再回落到过程区）。版本升至 **`0.1.106`**；GitHub Release 与 npm publish 均已完成，`codex-mobile-re@0.1.106` 为 `latest`。针对性回归 127/127、`vue-tsc --noEmit` 和生产构建均通过。详见 [round-59](rounds/round-59-v0.1.106-release.md)。
- **热点领域模块化已完成（round-62 收官 + round-63 hook 化）**：`codexGateway.ts` 已薄壳化为 11 个领域文件，`codexAppServerBridge.ts` 已完成 A~AF 批并从约 10,203 行降至 2,053 行。`useDesktopState.ts` 迁出 model/provider/reasoning、context、collaboration、rate limits、project organization、Skills/Hooks catalogs 与 Queue/Auto-compact 状态，配合 Settings/低频 UI 异步边界，主文件从约 4,766 行降至 3,927 行；其后按「读请求+缓存所有权」另批完成消息历史/线程标题缓存/待办服务端请求，最终降至约 3,483 行并**正式收官**（turn lifecycle / realtime 写引擎作为有意整体边界保留）。round-63 完成 ThreadConversation/App.vue 的 hook 化（见 [round-63](rounds/round-63-v0.1.108-hooks-and-release.md)）。详见 [round-62](rounds/round-62-domain-modularization.md) 与 [领域方案](sections/domain-modularization-plan.md)。
- **round-52 发布已完成（v0.1.103）**：`codex-mobile-re@0.1.103`——live 多 agent 去重（源 `upsertLiveAgentMessage` + 最终汇合 `dedupeAssistantAgentMessageText`）+ 补充修复（`buildTurnRenderGroups` `liveOverlayActive` 双守卫，防止 live 流式中已完成中间消息被误提升为 final 拉出过程区）；真机 DOM 直证验证通过（线程 01a024d1 流式中中间消息保持 `conversation-item-process`、结束后真正最终正常落成 `conversation-item-final`）；全量单测 + `vue-tsc --noEmit` 通过，`pnpm run build` 通过（web + CLI 均成功）。git tag `v0.1.103` → 版本/文档提交 `d9f5972`。**GitHub release `v0.1.103`** 见 https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.103（2026-08-22 创建，非草稿/非预发布，指向 main，使用 `gh release create`；本机已装 gh v2.98）；**npm 发包已完成**：`npm view codex-mobile-re@0.1.103` 确认 version 0.1.103 / dist-tags.latest 0.1.103。
- **前端大文件组件化已完成（round-48/49 四期）**：方案见 `docs/componentization-plan.md`。四期已全部落地并提交推送——一期侧栏线程行复用（`4fa26ba`：`SidebarThreadRow.vue` + Skills 标签）；二期 Directory Hub 标签拆分（`e28db82`：Apps/Plugins 标签；`d8e4d72`：Composio 标签）；三期 `App.vue` 设置/账号面板（`aa1839b`：`SettingsAccountsPanel.vue` 等）；四期 `ThreadComposer.vue`/`ThreadConversation.vue` 展示层（详见 [round-49](rounds/round-49-componentization.md)）。组件化本身已完成；被排除的逻辑模块按领域方案处理，其中 gateway/bridge 已完成，`useDesktopState` 仍在保守续拆。
- **round-47 过程区视觉层级与折叠交互、宽屏边距修复已完成并发布（v0.1.100）**：过程标题为 12px，过程 assistant 正文为 13px，final 区保持 14px 主阅读层级。含最终 assistant 回答的过程区默认展开，标题显示过程项数量并可点击收起；收起不隐藏用户请求或最终回答。执行中、尚未有最终回答的 turn 始终展开；同一已挂载 turn 新增过程项或文件变更时由数组长度监听自动展开，刷新后默认展开，折叠状态不持久化。`pnpm exec vitest run src/utils/transcriptGrouping.test.ts` 33/33 通过，`pnpm run build` 通过（既有主 chunk 大小警告）；浅色/深色 × 375x812/768x1024 四组 Playwright 检查确认折叠、展开、ARIA 状态、最终回答可见性、刷新默认展开与无横向溢出。性能审计：新增每个已渲染 turn 的局部布尔状态和条件列表渲染，未新增请求、消息分组、缓存或存储 I/O；运行时 profile 仍未获取，因为缺少 Playwright Chromium。**宽屏修复**：当 viewport 大于 1280px 时，过程区原先占满 turn 宽度而内部消息行居中，导致左侧轨道与内容之间出现大段空白；已在 `.conversation-turn-process` 增加与消息行一致的 `max-width` 和 `margin-inline: auto` 约束，使轨道在 720px 聊天列内对齐。修复后浏览器实测宽屏过程区为 720px，左侧轨道与 request/final 区对齐；`round48-process-collapse-check.cjs` 在浅色/深色 × 375x812/768x1024 四组均通过。**发布状态**：代码已推送至 `origin/main`（`fbeb6d8`），版本号 `0.1.100`，npm 已发布并成为 `latest`，GitHub tag `v0.1.100` 与 GitHub release 均已创建。
- **npm 发包已完成（v0.1.99）**：`codex-mobile-re@0.1.99` 已由用户发布到 npm 官方源并成为 `latest`（2026-08-19，`npm view codex-mobile-re@0.1.99` 确认 version 0.1.99 / dist-tags.latest 0.1.99）。GitHub release `v0.1.99` 见 https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.99；git tag `v0.1.99` → `e45505e`。
- **已推送**：`main` 与 `origin/main` 已同步至 `1e85cf4`（round-41/42 修复 `e1dccb9`/`a33395e`/`548983e`/`6378b34` 随 v0.1.95 发布，tag `v0.1.95`；round-43 修复 `ff6df2b`/`5aaa458` + 版本 `64fa15d` 随 v0.1.96 发布，tag `v0.1.96`；round-44 文档 `ad55eef` + round-45 修复 `7fa6ec4` + 版本 `b2074f3` 随 v0.1.97 发布，tag `v0.1.97`；round-46 fix `24a23dc` + 版本/文档 `47f3326` + 状态更正 `1e85cf4` 随 v0.1.98 发布，tag `v0.1.98`，`origin/main` 已同步）。推送方式：优先直连 GitHub（偶发成功）；直连失败时临时经本机代理 `git -c http.proxy=socks5h://127.0.0.1:10808 -c https.proxy=socks5h://127.0.0.1:10808 push`，未改全局 git 配置。注意：代理端口（10808/10811/10812）以 xray/v2rayN 进程是否存活为准，退出后端口即失效，直连即可（2026-08-06 晚实测代理全关、直连重试 3 次后成功）。2026-08-18 晚用户启用的代理是 **Clash，监听 `127.0.0.1:7890`**（非 10808/10811/10812）：先用 `netstat -ano | grep LISTENING` 找到实际监听端口，再 `git -c http.proxy=socks5h://127.0.0.1:7890 -c https.proxy=socks5h://127.0.0.1:7890 push`、gh 用 `HTTPS_PROXY=http://127.0.0.1:7890`。2026-08-11 round-34/35 推送直连成功（git 2.35.1，位于 `E:\Git`，不在默认 PATH，需前置进 PATH 或全路径调用）。gh release 需 `--repo cattails-lgao/codex-mobile` 指定本 fork（默认会命中 upstream remote）；prepublishOnly 会执行 `pnpm run build`，发包前把 `<pnpm 全局 bin 目录>` 加入 PATH
- **npm 发包已完成（v0.1.98）**：`codex-mobile-re@0.1.98` 已由用户发布到 npm 官方源并成为 `latest`（2026-08-18，`npm view codex-mobile-re@0.1.98` 确认 version 0.1.98 / dist-tags.latest 0.1.98）。GitHub release `v0.1.98` 见 https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.98。
- **npm 发包已完成（v0.1.96）**：`codex-mobile-re@0.1.96` 已由用户发布到 npm 官方源并成为 `latest`（2026-08-18）；`npx codex-mobile-re@0.1.96 --help` 验证可加载、完整 CLI 用法正常。GitHub release `v0.1.96` 见 https://github.com/cattails-lgao/codex-mobile/releases/tag/v0.1.96；git tag `v0.1.96` → `64fa15d`（release 首次创建时曾误指向旧提交，已强制改指到正确提交）
- **遗留记录**：2026-08-18 round-43 会话中，fnm 管理的 Node v24.18.1 `node.exe` 曾从安装目录消失（目录被清空），已手动重新下载同版本 zip 解压回 `installation/` 恢复；若再次遇到 node 找不到，先 `ls <fnm node-versions>/v24.18.1/installation/` 确认二进制是否在
- **工作区状态**：round-64 交接后 `main` 与 `origin/main` 同步（待推送 `71e7cd8` 及交接文档提交）；`git status --short` 含待推送的发布/文档改动
- **依赖安装历史**：若换机重新 `pnpm install`，观察 `allowBuilds` 是否完整覆盖构建需求；如出现新的「Ignored build scripts」警告，按同名格式补充到 `pnpm-workspace.yaml`
- **跨平台回归（2026-08-06 已完成 Linux 侧；2026-08-07 已完成 macOS 侧）**：Linux 侧已用本机 WSL2（Ubuntu）验证——`vue-tsc --noEmit` 无类型错误、`vite build` 成功（4.58s）、`tsup` CLI 构建成功、单测 20 文件 229 用例全部通过（Windows 侧基线为 227 通过 + 2 环境性失败，Linux 下无此环境性失败，全部通过）。macOS 侧验证见下方「macOS 侧跨平台回归（2026-08-07）」。WSL 环境配置：fnm 1.39.0（`~/.local/share/fnm`）+ Node v22.23.2 + pnpm 11.18.0；注意 WSL 内无 fnm 时需先装（本机 Windows fnm 仅含 Windows 版 Node，无法在 WSL 复用），验证目录 `~/codex-linux-check`（从 Windows 侧 rsync 源码，排除 node_modules/dist/output/.git 等）；WSL 内无法直连 fnm.vercel.app（超时），Node 二进制由 Windows 侧下载后经 `/mnt/c` 共享解压，fnm 1.39.0 二进制同理
- **已知限制（round-44，非代码改动）**：WebUI 对某线程发消息后会由 WebUI 的 app-server 进程持有该线程的 writer 锁（OS 文件锁，`CODEX_HOME/thread-writer-locks/<threadId>.lock`），直到 WebUI 重启；随后在 TUI 打开同一线程会报 `thread/resume ... already has an active writer (code -32600)`。纯只读预览（`thread/read`）不抢锁、安全；`ExternalSessionTracker` 只读查看也不抢锁。该 codex 版本无释放 writer 的 RPC。详见 [rounds/round-44-feedback.md](rounds/round-44-feedback.md)
- **验收遗留**：plan/approval popover 键盘导航已补齐（见「遗留项补齐（2026-08-07）」）；ExecPlans 待后端 Codex 版本支持后自动变为可选；macOS 侧跨平台回归已完成（见「macOS 侧跨平台回归（2026-08-07）」）
- **本轮测试遗留（非仓库文件）**：验证脚本在 `<外部测试目录>\hello.txt` 创建了一个测试文件（不在仓库允许操作范围内，未能自动清理），如不需要可手动删除；测试期间创建的 5 个临时线程已通过 `thread/delete` 清理

## 交接注意事项

- 本项目在多个环境（Windows/macOS/Linux 及不同机器）运行，**工具链位置不写死**：node/pnpm/codex-cli/git 等一律用语义占位（`<node 安装目录>`、`<pnpm 全局 bin 目录>`、`<Git 安装目录>` 等），不使用任何具体路径骨架（含脱敏后的 `C:\Users\<用户名>\...`）；需要定位时用命令探测（`Get-Command`、`pnpm bin`、`npm prefix -g` 等），并给出探测命令而非路径
- 文档与示例中的用户名、本机绝对路径均已脱敏，用户名用 `<用户名>`、项目根目录用 `<项目目录>`、外部目录用 `<外部测试目录>` 占位
- 不要重新创建被删除的 `vite.config.js` 等本地产物，仓库只用 `vite.config.ts`
- `5173` 是 HBuilderX 的服务，不要随意停止或改动
- dev server 用 `--host 127.0.0.1` 启动，避免局域网暴露；如需远程访问再按需调整 host
- git 提交信息遵循仓库惯例（具体、单任务、不混提无关改动）

---

*codexapp · 交接文档 · 2026-09-16（round-86：上翻更早轮次改为游标分页（去掉最后一处全量水合）——`readThreadForTurnPage` 仍发 `thread/read {includeTurns:true}` 再内存 slice，实测 1295ms / 12.12MB 而 `includeTurns:false` 只要 2ms / 0.00MB；并纠正 round-84/85「顶层 `turnsBackwardsCursor` 是该用的游标」这一错误措辞——它 `includeAnchor:true`、用它只会重发自己那一页，真正往更老走的是 `initialTurnsPage.nextCursor`，且游标是不透明 JSON、turn id 不是游标，故没有一次性「取某轮之前一页」的调用、只能逐页串链；设计为「升序 id 列（`turns/list{10000,notLoaded,desc}`，不缓存）+ 游标链（threadId→(turnId→更老游标)，来源=打开会话那页 + 本路由服务过的每页）+ 页长/页首页尾校验」，任何意外返回 null 回落原全量水合且回落路径逐字相同；元数据等价性先验（`thread/read {includeTurns:false}` 的 `thread` 32 键全同、仅 `turns` 空），新增单测 24 例、`scripts/probe-turn-page.cjs` 11/11 PASS、`git stash` 端到端 A/B 四锚点响应逐字相同（含字节数）、真实 Linux（WSL2）全量 649/649 通过（关闭既有的 2 例 Windows 环境性失败注记）、`SHARED_BRIDGE_VERSION` v3→v4；round-85：会话日志时序恢复的适用范围——round-84 之后 app-server 侧只剩 198ms、经桥仍 734–1242ms，逐段量出最大一段是命令合并 434–681ms（读 167–295 + 解析 220–348），而 CLI 已把命令从 `function_call exec_command` 换成 `custom_tool_call exec`，当前形状日志识别到 0 行；没有命令槽时它仍把 user/agent 提前、命令追加轮末，反向破坏 app-server 已正确的交错——在旧形状上它是在修 bug，在新形状上它自己就是 bug；改为形状闸门（无命令/文件变更槽即原样返回、全无改动返回入参同一引用）+ 版本判据缓存（判据为假只付一次 stat 不读文件、size/mtime 变则重判），同环境 `git stash` A/B 热态中位 1071→610ms（−43%），条目顺序与「跳过合并」基线 10/10 逐字一致，旧形状两会话仍 58/58 与 51/51，判据由 `scripts/probe-session-log-recovery.cjs` 固化为可复跑脚本；round-84：打开会话去掉全量历史水合——用户口径「慢的不是 DOM，等待时间全在 RPC」；`thread/resume` 让 app-server 全量水合后被桥裁到 10 轮，实测 2118ms / 12.12MB 全部白付（`includeTurns:false` 只要 2–3ms，成本全在构造轮次），而协议原文已把全量水合标为 deprecated 并指向 `excludeTurns` + `initialTurnsPage`；改为有界页后桥把返回页反转成升序提升为 `thread.turns`、补 `threadTurnStartIndex`、丢弃 `initialTurnsPage`，前端与既有管道零改动，同桥 A/B 2.0–3.1× 且输出逐字一致，旧 app-server 忽略该字段时按原请求重放兜底，协议假设由 `scripts/probe-resume-turn-page.cjs` 固化为可复跑脚本；round-83：客户端自动压缩在长 turn 下不触发——阈值此前只被「发送 + 空闲」消费，turn 内按设计跳过、turn 结束又只补发暂存不做压缩，改为在 turn 转空闲那一刻按同一阈值预检（实测服务端本就在 turn 内 ~90% 处自己压缩，客户端默认阈值 10% 剩余与之同点，本轮按用户决定维持 10%）；round-82：systemd 停止超时——websocket 升级后的 socket 脱离 http server 的 tracked connections，`closeIdleConnections()` / `closeAllConnections()` 都够不到它，`server.close()` 永不回调，于是每次 `systemctl restart` 都以失败行收场（`exit-code` 与 `timeout` 交替）；改为显式 `terminate()` 客户端 + 补齐连接清理，并把 5s 兜底提前注册；round-81 / round-80 / round-79 已随 v0.1.123 同批发布，round-82 随 v0.1.124 发布，npm publish 由用户执行；随后在打包产物上补做端到端 A/B（修复前 exit 1 @ 5015ms、修复后 exit 0 @ 11ms），再于 WSL2 真实 Linux 上用真实 SIGTERM 与真实 systemd 复测——pre-fix `Failed with result 'exit-code'.` 逐字复现、post-fix 无失败行，整条停止链路闭环）· 内容已脱敏*
