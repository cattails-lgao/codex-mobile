# Round-24 交接需求（2026-08-08 提出）

> **2026-08-08 进展：** 7 条反馈/问题全部落地并验证：`vue-tsc --noEmit` 通过、`pnpm run build` 通过、全量单测 312/312（新增 2 例：textDelta 增量通道按 item 粒度存档 + `commandExecution` 映射为 Running command overlay 标签）、Playwright 浅色 15/15 + 暗色 13/13 通过（Edge channel，mock 线程）。涉及 `ThreadConversation.vue`、`MessageToolbar.vue`、`ReasoningBlock.vue`、`LiveOverlayItem.vue`、`ToolCallRow.vue`、`useDesktopState.ts`、`useDesktopState.test.ts`、`style.css`。手动测试文档 `tests/chat-composer-rendering/round24-message-toolbar-reasoning-filechange-title-truncate.md`，回归脚本 `output/playwright/r24-verify.cjs`（浅色）与 `output/playwright/r24-dark.cjs`（暗色）。

1. **用户消息下回退按钮与复制按钮统一风格并加大**：两个按钮此前尺寸偏小且风格不一致（复制按钮带边框背景、回退按钮无）。统一为朴素图标按钮：`inline-flex items-center gap-0.5 rounded-full px-1.5 py-1 text-[11px] font-medium leading-none transition`，无边框无背景，图标 `text-sm`；复制按钮保留 `data-copied` 绿色已复制态。暗色覆盖同步（复制 zinc-400、回退 amber-400/80）。
2. **思考过程块样式改为与 Running command 类似**：`ReasoningBlock` 去掉卡片（`rounded-xl border bg-zinc-50/80`），改为 `w-full min-w-0` 裸行；header `px-0 py-0.5`，标题 `text-xs font-medium` + `#737373`，body 去 border-t，摘要/内容 `#737373`，与命令块同视觉密度。
3. **Running command 下 `live-overlay-details` 多余**：命令文本已显示在消息列表的 WorkBlockItem 中，overlay 内再重复展示一遍为冗余。`LiveOverlayItem` 增加 `overlay.activityLabel !== 'Running command'` 条件隐藏 details；Thinking 等其他状态不受影响（r23 回归确认详情 chips 仍正常）。
4. **fileChange 块在当前会话轮结束后展示在最后**：锚点从「最后一个 assistant 文本」放宽为「该轮最后一条实质渲染消息」（命令/工具/assistant 文本均可），`anchoredFileChangeSummaryByAnchorId` 按新锚点聚合；inline 版从 article 内移出，改为 li 级独立块渲染（`</article>` 之后、MessageToolbar 之后），fold 内用 `foldTailMessage` 在折行尾部渲染；`standaloneFileChangeSummaryByMessageId` 增加 `anchoredSourceIds` 互斥，避免锚点放宽后同一轮重复渲染。
5. **思考过程块刷新后全部跑到开头**：真实 app-server 的 reasoning 常走 `item/reasoning/textDelta` 增量通道（无伴随 `item/started` 的 reasoning 项），`turnItemSequenceByThreadId` 没有 reasoning 条目导致回退整段存档、刷新后全部插到轮首。修复：`recordTurnItemOrder` 支持 `item/reasoning/textDelta`/`item/reasoning/summaryTextDelta`，通过 `params.itemId` 按 reasoning 类型记录 item 粒度时序；`readReasoningDelta` 返回 itemId；textDelta 累积进 `reasoningItemTextByItemId`；新增单测验证生成 `reasoningAnchorMessageId:"cmd-1"`。Playwright 验证刷新后思考仍插在命令之后。
6. **线程名称未截断到 20 字**：`thread/name/updated` 通知直接用 app-server 推送的完整用户消息标题，缓存加载与展示层也未截断。三条路径统一收口：通知写入前 `toOptimisticThreadTitle` 截断、`loadThreadTitleCacheIfNeeded` 加载缓存时逐条截断、`applyCachedTitlesToGroups` 无论缓存命中与否都截断。Playwright 验证侧边栏展示标题长度 = 20。
7. **round-23 颜色/字体规范在工具块未实现、无法区分**：`ToolCallRow` 仍用卡片样式（浅灰背景 + 边框 + 8px 圆角），与命令块视觉不一致导致「颜色字体无法区分」。去卡片化：`.tool-call-block` 改 `flex w-full min-w-0 items-center gap-2 px-0 py-0.5`，删除带边框的 running/ok/error 类，仅保留状态文字色；暗色覆盖同步（名称 zinc-200、状态 zinc-400、running amber-400、ok emerald-400、error rose-400）。

> **验证说明：** Playwright 浅色 15/15——回退/复制按钮尺寸（宽 26/高 22）+ 无边框无背景 + 风格统一、思考块透明无边框无圆角 + 标题 `#737373`、工具块去卡片化 + 工具名 `#737373`、正文/标题/加粗色值字号（`#171717`/14px、`#17181a`/16px）、fileChange 块位于 agent 回复之后（轮末）、线程名 ≤20 字、带 anchor 思考刷新后仍在命令之后。暗色 13/13——回退 amber-400/80、复制 zinc-400、思考标题/内容 zinc-400、摘要 zinc-500、工具名 zinc-200、状态 zinc-400、块均透明无边框无圆角。截图 `output/playwright/r24-*.png`（含暗色 `r24-dark-q1-q2-q7.png`）。
