# 第十八轮交接需求（2026-08-08 提出，round-23）

> **2026-08-08 进展：** 10 条反馈/问题全部落地并验证：`vue-tsc --noEmit` 通过、`pnpm run build` 通过、全量单测 310/310（新增 2 例 `mergePersistedReasoning` 时序锚点单测）、Playwright（Edge channel，mock 线程）断言 23/23 通过、桥接层思考存档 PUT/GET 实测可用（写入、读取、清空）。涉及 `ThreadConversation.vue`、`LiveOverlayItem.vue`、`MessageToolbar.vue`、`ReasoningBlock.vue`、`WorkBlockItem.vue`、`ToolCallRow.vue`、`ProcessFold.vue`、`ThreadComposer.vue`、`ThreadPendingRequestPanel.vue`、`App.vue`、`useDesktopState.ts`、`types/codex.ts`、`api/codexGateway.ts`、`server/codexAppServerBridge.ts`、`style.css`、`useDesktopState.test.ts`。手动测试文档 `tests/chat-composer-rendering/round23-message-fonts-overlay-toolbar-plan-pending.md`，回归脚本 `output/playwright/r23-verify.cjs`。

1. **思考过程按真实时序渲染 + 跨浏览器持久化**：两处根因——①存档把整轮 thinking 存成一条消息插在轮次开头（工具/命令仍留在流里），造成「思考集中在每轮开头」；②app-server 不把 reasoning 持久化到 thread/read，存档只写 localStorage，换浏览器即丢。修复：按 `item/started|item/completed` 到达顺序记录每轮「推理项/工具项」时间线（`turnItemSequenceByThreadId` + `reasoningItemTextByItemId`），轮次结束时按 item 粒度存档，每条思考带 `reasoningAnchorMessageId`（该思考之前最近一个工具/命令项的 id）；`mergePersistedReasoning` 支持锚点插入（锚点缺失时回退到按轮次插入），`mergeThreadMessageStreams` 先合并 persisted+live 再插思考（turn 刚结束时锚点也能命中 live 命令）。跨浏览器：桥接层新增 `/codex-api/thread-reasoning` GET/PUT（存到 `.codex-global-state.json`，与 thread-titles/pins 同源，每线程最多 20 条），`savePersistedReasoningMap` 保存时镜像到桥接层，启动时 `loadThreadReasoningArchiveIfNeeded` 按文本去重合并恢复（localStorage 兜底）。单测新增「锚点插到工具之后」与「锚点缺失回退」两例。
2. **字体规范**：正文 14px / `#171717`，标题统一 16px / `#17181a`（h1–h6 去掉逐级字号与 uppercase），加粗 `#17181a`，工具与思考文字 `#737373`（`work-block-command`/`tool-call-*`/`process-fold-label`/`reasoning-block-*`/`live-overlay-*`）；暗色覆盖保持原有 zinc 色。
3. **用户消息下的回退改为仅图标 + 新增复制按钮 + 默认常显 + 整体右对齐**：`MessageToolbar` 对 `data-role='user'` 增加 `opacity: 100; self-end`（无需 hover）；回退按钮 label 在 user 角色下隐藏（仅图标）；`ThreadConversation` 新增 `isCopyableUserMessage`/`copyUserMessage`（复制用户消息文字 + 附件/图片），user 消息显示复制按钮。
4. **线程名用第一条消息总结并限制 20 字**：`generate-thread-title` RPC 已有，但结果未截断；`requestThreadTitleGeneration` 现在把总结结果收口到 20 字（`OPTIMISTIC_THREAD_TITLE_MAX`），占位名 `toOptimisticThreadTitle` 也从 80 字改为 20 字。
5. **live overlay 与其他消息左对齐**：`LiveOverlayItem` 的 scoped CSS 补齐 `.message-row`（`w-full max-w-[min(var(--chat-column-max,45rem),100%)] mx-auto flex`）与 `.message-stack`——ThreadConversation 的 scoped 样式不会进入子组件，缺 `mx-auto` 导致 overlay 与其他消息不对齐；同时消除父级 `.conversation-item-overlay { justify-center }` 对子 flex item 的居中影响。
6. **询问面板下拉框无法点击**：根因——面板用 `transform: translateX(-50%)` 居中，transform 成为 fixed 后代的包含块，面板内 `ComposerDropdown` 的 fixed 定位菜单按视口坐标计算后被整体偏移。改为 `left:0; right:0; margin-inline:auto` 居中，菜单定位恢复；Playwright 实测下拉可打开、可选中其他值。
7. **plan popover 内容优化**：摘要取第一句（按中英文句号/换行截取，超 80 字加省略号）+ `line-clamp-2`；步骤项单行省略（`truncate` + `items-center`）；执行按钮移到固定 footer（`.thread-composer-plan-panel-popover-footer`），popover 改为 flex 列布局、内容区 `overflow-y-auto`、按钮不再随滚动消失。
8. **点击执行后 Thinking 水平居中/无内容/不知道模型在干什么**：对齐随第 5 条修复（overlay 行补齐 mx-auto 后左对齐）；`LiveOverlayItem` 新增活动提示——运行中无思考内容时显示 spinner（`.live-overlay-spinner`）+ 活动详情 chips（`.live-overlay-detail`：Mode/Model/Thinking/Speed），等待阶段不再是一片空白。
9. **审批/询问面板与输入框同宽 + 命令块内部滚动**：`updateComposerShellWidth` 按继承的 `--chat-column-max` 收口（宽屏下面板不再比输入框宽）；`.thread-pending-request-command-line`/`.thread-pending-request-preview` 加 `max-h-28 overflow-y-auto overflow-wrap:anywhere`，长命令在块内滚动。
10. **（问题）审批面板发送/跳过点了没反应、刷新后面板消失**：根因——请求已被服务端解决（超时自动拒绝、TUI/另一浏览器已应答、app-server 重启）但本地没收到 `server/request/resolved`，残留死面板；回 RPC 报错但面板留着；刷新后 pending 列表已空所以面板不再出现（预期行为）。修复：回复失败时调用 `pendingRequestStillExistsOnServer` 与服务端待办列表对账——服务端已无该请求则移除本地残留（面板正常关闭）；仍存在则把错误展示在面板内（`panel-error`，新增 `pendingReplyErrorForRequest`），不再无声失败；面板 z-index 900→1200（高于右侧面板 resize 手柄 z-1100，避免手柄条挡住按钮）。

> **验证说明：** Playwright 23/23——字体色值/字号、用户操作条 opacity=1 + 仅图标回退 + 复制按钮 + 右对齐、overlay 与消息 x 一致（411=411）+ spinner + 详情 chips、plan popover 一句话摘要 + 步骤 ellipsis + footer 按钮 + 内容区滚动、审批面板宽 720=输入框 720 + 命令块 `maxH=112px overflow=auto`、面板 transform=none、下拉可开可选、过期请求点击发送后面板自动关闭；桥接层 `/codex-api/thread-reasoning` 写入/读取/清空 curl 实测通过。截图 `output/playwright/r23-*.png`。
