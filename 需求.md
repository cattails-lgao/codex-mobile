# codexapp 外部会话（TUI）working 状态支持 — 需求清单

日期：2026-08-02
状态：待实施（基于 codexapp 0.1.90 源码）

## 背景

codexapp 的 webui 只把 app-server RPC 返回的线程状态翻译成界面状态。TUI 是独立进程，直接写 session 日志（JSONL），app-server 无法得知 TUI 的 turn 正在运行，只能把这类线程物化为 `notLoaded`、把进行中的 turn 标记为 `interrupted`，因此 webui 永远不显示 working。

本需求**不改 app-server 的数据语义**，而是在 codexapp 这一层叠加"外部会话活性检测"，把 TUI 正在跑的状态翻译成前端认识的 working。

## 一、核心功能需求

### 1. 外部会话活性检测

对每个线程，直接读它的 session 日志文件（`thread.path` 指向的 JSONL），判断"当前有没有一轮在跑"：

- 日志里存在 `event_msg/task_started`，且对应的 `turn_id` 没有 `task_complete`；
- 文件仍在持续写入（mtime 距今 < 可配置窗口，默认建议 15–30 秒）；
- 两个条件同时成立 → 判定为 working。

需要增量解析：缓存每个文件的已读行数和 mtime，只读新增部分，避免轮询时反复扫全量文件（会话日志动辄几 MB）。

### 2. 会话归属识别

从日志首行的 `session_meta.originator` 区分：

- `codex-tui` → 外部会话，走活性检测；
- app-server 自己创建的线程 → 以 RPC 返回的 status 为准，不做叠加。

TUI 的子代理线程（`thread_source: subagent`，父线程是 TUI）也要继承 working 状态。

### 3. 状态叠加层（不污染 app-server 数据）

在 codexapp 的 API 响应里合并检测结果，建议输出显式字段：

```json
"externalSession": {
  "origin": "codex-tui",
  "active": true,
  "lastWriteAt": "2026-08-02T20:50:00.000Z"
}
```

不要把 app-server 返回的 status 直接改掉，保留原始真相，前端和后续功能都好扩展。

### 4. 停止按钮的语义

外部会话的 turn 归 TUI 进程所有，app-server 无法真正 interrupt。功能上二选一：

- 简单版：检测到 externalSession 时，前端禁用停止按钮，显示"该线程在 TUI 中运行"；
- 完整版：codexapp 提供"发送停止信号"的能力，但需要 TUI 侧配合（见第六节），否则只能做到"提示"。

建议先做禁用+提示。

### 5. 提交/排队语义

外部会话正在跑时，webui 在这个线程发消息没有意义（app-server 会拒绝 active turn）。要禁用 composer 并提示。若未来想支持排队，可复用现有 `thread-queue-state` 机制，但属后话。

## 二、后端改动点

| 位置（源码） | 改动 |
|---|---|
| 新增 `src/server/externalSessionTracker.ts` | 活性检测器：文件扫描器/轮询、JSONL 增量解析、状态缓存、启动时全量扫描一次 |
| `src/server/httpServer.ts` — `thread/list` 处理器（dist 约 10233 行） | 给每行 thread 叠加 `externalSession` 字段（侧边栏 working 圆点靠它） |
| `src/server/httpServer.ts` — `/codex-api/thread-live-state`（dist 约 10932 行） | `isInProgress` 改为 `app-server状态 OR 检测器结果`；同时返回 externalSession |
| `src/server/httpServer.ts` — `thread/read` 相关代理（`appServer.rpc` 封装处） | 同样叠加，保证打开会话的 `inProgress`/`activeTurnId` 一致 |
| `cacheLiveState` / `getCachedLiveState`（dist 约 10970 行） | 外部会话在跑时**跳过缓存**；或缓存带 mtime，文件一变化就失效，否则侧边栏会卡在旧状态 |
| `/codex-api/events` SSE | 检测器状态翻转（idle ↔ working）时推一条 `externalSessionChanged` 事件，前端可即时刷新，不依赖轮询 |

## 三、前端改动点

- **ThreadConversation / 会话页**：`inProgress` 判断已存在（`gv()`/`Md()`），只要服务端返回正确字段就自动显示 working；额外做：externalSession 时禁用停止按钮、禁用 composer、加"TUI 运行中"提示条。
- **侧边栏 ThreadTree**：现有 `kr()` 已支持 `inProgress → "working"` 徽标；建议 externalSession 用不同颜色（如琥珀色）区分"TUI 在跑"和"webui 自己在跑"。若不做 SSE，加一个 5–10 秒的列表轮询兜底。
- **打开/关闭线程时**：如果 app-server 返回 notLoaded（它还没物化这个 TUI 线程），前端要能容忍——现在已经在容忍了，但叠加后确保不闪回 idle。

## 四、可配置项（建议用环境变量起步）

- `CODEXUI_EXTERNAL_SESSION_TRACKING`：总开关，默认开；
- `CODEXUI_EXTERNAL_WINDOW_MS`：活性窗口，默认 30000（30 秒无写入判定停止）；
- `CODEXUI_EXTERNAL_POLL_MS`：检测轮询间隔，默认 3000；
- 是否只跟踪 `originator=codex-tui`，还是所有非 app-server 来源。

## 五、必须覆盖的边界情况（写进测试）

1. TUI 跑任务 → working 出现；TUI 结束（task_complete 落盘）→ 自动回 idle。
2. TUI 崩溃残留：最后一轮无 task_complete，但文件已停止写入 → 窗口到期后回 idle，**不能永久 working**。
3. webui 自己启动的线程不受影响（originator 不是 codex-tui）。
4. TUI 的子代理线程同步显示 working。
5. 已归档会话（`archived_sessions/`）不参与检测。
6. 会话日志被 compact/归档时状态不崩、不误报。
7. 大文件轮询性能：增量解析后，1MB 日志的轮询开销 < 几毫秒。
8. 多客户端同时看同一线程，状态一致。

## 六、可选增强（需要 TUI 侧配合，先不做也行）

如果将来想让 webui 的停止按钮真正生效，需要 TUI 启动时写一个 `owner.json` 到会话目录（内容：pid、TTY），codexapp 检测到后可以向该 pid 发 SIGINT。这属于 TUI/wrapper 的改动，不是 codexapp 源码的事——所以列为可选项。

## 七、最小可交付版本

如果只想最快看到效果，砍到三件事就够了：

1. `externalSessionTracker`（活性检测核心）；
2. `thread/list` + `thread-live-state` 两处叠加字段；
3. 前端"TUI 运行中"提示 + 停止/输入禁用。

其余（SSE、颜色区分、配置项、owner.json）都可以后补。核心代码量约 300–500 行，含测试。
