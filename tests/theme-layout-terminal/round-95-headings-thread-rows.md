# Round-95 手测：标题层级 + 线程行（时间/导轨/pip）+ 区头降噪

> 自动化覆盖之外的观感与行为确认。采样线程 `01a0238c-d5e7-7651-97cc-2303f044f00e`（含 4 个 h2）。

## 前置

- 生产构建服务：`http://127.0.0.1:4190`（构建后刷新即生效）。
- 记录方式：逐项核对后勾选；偏差记入「偏差」小节。

## 步骤与预期

1. **标题层级**：打开采样线程，找消息里的 `##` 标题。预期：约 18px 加粗、`--ink-1`，明显大于 14px 正文；连续多个标题时 h2 > h3 > h4 递减可辨。
2. **标题跨主题**：切亮色主题重复。预期：标题与正文色差方向一致（标题更深），无裸灰硬编码的突兀感。
3. **线程行·相对时间**：侧栏线程行右侧时间（如 `33d`）应为**等宽**字体、12px、与标题行基线对齐；肉眼对比两个相邻行时间应上下对齐（tabular-nums）。
4. **线程行·选中导轨**：点选任一线程。预期：该行表面抬升（`--s2`），左缘出现 2px 中性圆头导轨；琥珀色**不出现**在选中态。
5. **线程行·运行 pip**（需一个运行中的线程；本机没有时用模拟线程或跳过行为项只看样式）：运行中的线程行左缘导轨为琥珀（`--live`），行内状态指示为 6px 琥珀圆点带呼吸（不是转圈边框）；`prefers-reduced-motion: reduce` 下不动画。
6. **区头降噪**：Chats/Projects 区头行 hover 为表面抬升（无突兀的深灰 `zinc-200` 硬跳）；New chat 动作按钮 hover 同样走 token。
7. **暗色专项**：暗色下重复 3-6。预期：线程行标题 `--ink-2`、选中行 `--ink-1`；h6 标题保持暗色专有的更弱一级（有意设计）。

## 偏差

（无）
