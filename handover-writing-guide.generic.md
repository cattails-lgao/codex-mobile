# 交接文档写作规范（通用版）

> 本文档定义一套与具体仓库无关的交接文档组织方式与写法约定，可整体复制到任何项目使用。交接文档的目标是让**下一个会话或接手人只读文档就能接续工作**：知道当前状态、最近改了什么、为什么改、还剩什么、怎么启动验证。
>
> 使用方式：把本文档复制到目标仓库的交接文档根目录（如 `<repo>/handover/`），按其约定维护交接文档。若目标仓库已有自己的交接惯例，以仓库内说明为准，本文档作为补充框架。

## 1. 交接文档要解决什么

接手一个正在进行的项目时，最大的成本是「重新建立上下文」。代码、提交历史、测试都在，但以下信息不会自动浮现：

- 当前环境什么状态、怎么启动、有哪些坑
- 最近几轮改动分别做了什么、为什么这么做
- 哪些问题修了、哪些是调研结论、哪些还挂着
- 改功能时有哪些必须遵守的联动规则（测试文档、提交习惯、推送方式）

交接文档就是把这几类信息以固定结构沉淀下来。它不重复代码里已有的内容，只记录「不读文档就不知道」的部分。

## 2. 目录结构

建议的目录布局（`<repo>/handover/` 或仓库约定的交接目录）：

```
handover/
├── handover.md          # 交接总入口：快照 + 索引 + 常驻信息
├── rounds/              # 按轮次的逐轮记录（每轮一个文件）
│   └── round-NN-<主题>.md
├── sections/            # 跨轮次的公共主题
│   └── <主题>.md
└── handover-writing-guide.md   # 本文档（可选，复制过来供后续会话参考）
```

| 位置 | 放什么 | 不放什么 |
|---|---|---|
| `handover.md` | 当前快照、启动方式、未完成事项、注意事项、到各文件的索引 | 单轮需求细节、根因分析、验证过程 |
| `rounds/round-NN-*.md` | 某一轮验收/反馈的完整记录 | 环境配置、跨轮方案 |
| `sections/*.md` | 环境说明、提交历史、跨平台回归、长期方案等公共主题 | 单轮事件流水账 |

**总原则：** 每轮新增一个 `rounds/` 文件，不往已有轮次文件里追加新轮内容；公共主题进 `sections/`；总入口是查看文档的第一站，所有变更同步更新它。

## 3. 总入口 `handover.md` 的写法

### 3.1 当前快照

文件开头放一个固定表格，字段保持稳定，方便逐轮对比：

| 项 | 说明 |
|---|---|
| 分支 | 当前分支 + 是否已与远端同步 |
| 端口/入口 | 开发端口或访问入口（多个环境分别列） |
| 运行状态 | 运行中 / 未运行，健康检查结果 |
| 工具链 | 语言运行时 / 包管理器 / 关键 CLI 的版本与安装方式 |
| 最近提交 | 最近提交哈希 + 一句话说明（含「本轮改动待提交」标记） |

### 3.2 文档索引

用「环境与主题」「按轮次记录」两个表格罗列 `sections/` 与 `rounds/` 文件，每行给链接和一句话主题。新增文件必须同步登记，删除文件必须同步删链接。

### 3.3 常驻章节

- **环境与启动**：普通环境 / 受限环境（沙箱、容器）/ 跨平台各自的启动方式，含 PATH、环境变量、端口冲突规避。
- **未完成事项**：尚未推送的提交、未跟踪文件、依赖安装历史、跨平台回归状态、测试遗留。
- **注意事项**：脱敏占位规则、禁止重建的本地产物、端口与进程约束、提交与推送惯例。

### 3.4 落款

文件末尾保留一行 `*<项目名> · 交接文档 · <日期>（<最近进展一句话>）*`，随更新同步修改。

## 4. 轮次记录 `rounds/round-NN-*.md` 的写法

### 4.1 命名

- 前缀 `round-NN-`（两位数，如 `round-05-`）。
- 后缀英文短主题，连字符分隔，按内容类型选择：`feedback`（反馈）、`fixes`（修复）、`handover`（交接需求）、`backlog`（待办）、`refactor`（重构）、`upstream`（上游移植）等。

### 4.2 结构模板

每条需求/问题按固定顺序写，缺一不可：

```markdown
# Round-NN：<中文主题>（YYYY-MM-DD）

> **背景：** <来源：哪条线程 / 哪个环境 / 什么现象，为什么做>

## 1. <需求或问题一句话>

**现象：** <用户原话复述或现象描述>

**根因（实测确认 / 代码确认）：** <必须给出依据：真实数据、日志、代码路径分析；禁止猜测>

**修复（<涉及文件>）：** <改动点、为什么这样改；有设计取舍时说明备选>

**验证：** <单测 / 构建 / 端到端结果；异常基线要标注>

## 2. <下一项> …
```

若某条是**调研结论**而非修复，明确写「**调研结论：**」，给出结论依据与是否留待后续。

### 4.3 收尾验证说明

轮次末尾汇总验证口径：

- 类型检查是否通过；
- 构建是否通过；
- 全量单测通过数 + 失败数，失败注明是否既有环境性失败（给出具体用例）；
- 端到端断言数与截图路径；
- 涉及文件清单（含新增文件）。

## 5. 公共主题 `sections/*.md` 的写法

- 每个主题一个文件，命名 `小写-连字符.md`。
- 文档自包含：背景、方案/现状、结论、实施记录、评估。
- 跨轮次进展在「实施记录」节追加，不新建重复文件。
- 长期方案类文档保留「已完成 / 待办」状态标记。

## 6. 配套联动规则

1. **测试文档联动**：功能变更时，在 `tests/<domain>/` 新增或更新对应手动测试文档（命名 `round-NN-<主题>.md`），并在根测试索引与域索引登记。根索引只做目录。
2. **提交惯例**：每个离散任务单独提交，提交信息具体描述变更，不混提无关改动；代码修复与文档更新推荐分开提交。
3. **推送方式**：默认直连；直连失败时按仓库约定的代理或重试策略推送，不修改全局配置。
4. **脱敏**：用户名、本机绝对路径用 `<用户名>`、`<项目目录>` 占位；可保留便于复现的提交哈希、线程 id。
5. **可复现性**：根因结论给出复现路径（数据文件、构造方式、临时测试），接手人可自行验证。

## 7. 写完后自查

- [ ] 总入口快照表已更新（最近提交、运行状态）
- [ ] 新增的 `rounds/` / `sections/` 文件已登记到总入口索引
- [ ] 每条需求含「现象 → 根因 → 修复 → 验证」
- [ ] 验证口径齐全（类型检查 / 构建 / 单测 / 端到端）
- [ ] 功能变更已联动手动测试文档并登记索引
- [ ] 未完成事项已更新（未推送、遗留、未跟踪文件）
- [ ] 落款日期与进展已更新，内容已脱敏

---

*交接文档写作规范（通用版）· 复制到目标仓库后按需调整目录名与项目名*
